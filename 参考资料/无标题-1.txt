1.日历的周、日视图下拖拽仍然不正常：
1.只能往上拖，且释放后就把这个任务移过去了，而不是调整开始时间；这是有区别的，移过去是结束时间跟着变化把A时间段的任务移动到了B时间段，而调整时间是当前的结束时间不变，只调整开始时间相当于拉长了这个项目占用的时间（也就是说当前上下拖拽实际上运行的是移动项目的功能并不是正确的上下拖拽功能）；
2.右侧面板中截止日期还是没有增加时间单独调整的控件【这里需要注意，要想正确的使用日历视图，就必须有两个字段即开始日期和结束日期】时间参数只是针对日视图下使用，也可以不需要时间参数，但是这样的话这个项目解耦贯穿这个日期了，而不是在某个时间段；而且就不会显示在对应的时间这一行，而是显示在最顶部，与飞书中的甘特视图一样；


现在在今天的最新版上进行优化：
1.可以批量设置字段属性，如图示例效果，这样可以一次性快速wield各个字段进行设置操作
2.当前必须选择正确的开始和结束字段，然后这两个字段才能出现日期和时间的输入控件，这个是不合理的，直接设置为默认开启状态，只不过在添加数据的时候默认使用0:00的时间
3.飞书的甘特视图中日、月、周视图必须通过视图切换按钮来实现，然后每一个任务在月视图下是可以横向拖动的（也就是改变任务的开始时间和结束时间，拉长或压缩任务占时），只有在日、周视图下是可以纵向拖动的，即改变任务的开始结束时间。而且拖拽后的日期时间要实时保存写回Excel数据中。当前已经实现了在日视图的纵向拖拽功能，现在我还想实现周视图的纵向拖拽和月视图下的任务横向拖动功能，帮我分析并完成。【这里的拖拽交互逻辑和对应的参数比较重要，要单独写一份MD文档进行说明】
4.我想添加一个按指定字段进行自动分色显示的功能，用来对日历视图中的多个任务进行分色显示。我的构思是假设我按姓名字段为基准进行分色，那么在这个日历视图的各视图中每个人的任务都使用这个色调的颜色进行显示，这样便于一眼看出来哪些任务是谁的，是否任务过多，需不需要调整等这些都一目了然。我还想把这个功能加到卡片视图中，先做一个初版我看一下效果怎么样。
5.看板视图中有两个问题：1.拖拽的时候会出现选中拖拽区域文本的效果，2.拖拽完成后，各分组数据还是无法自动实时更新，必须单击刷新才可以
6.移除各个视图中的新增分组按钮及功能，另外把视图设置后面的组合框移到视图设置中去，这个是切换看板标题字段的
7.仪表盘单击后除了顶部的功能按钮有切换，其它区域都还是甘特视图的界面
卡片视图中的图片功能，选择一个文件夹，用户将每条记录的指定字段与此文件夹中的图片进行关联，既可按照这个字段自动匹配并显示器图片
8.表格视图中右侧的字段设置窗口还是无法与光标单元格的列进行实时联动
9.我发现在甘特视图下切换月和周视图的时候，顶部的标签有变化，但是下面的甘特图并没有变化，是这个按钮调用的功能不正确吗？
10.帮助信息中有MultiTable · 基于 Excel/WPS 超级表的本地多维表分析工具，当前插件已经适配了wps的使用环境吗，如果没有就要修改这里的信息
11.Excel选项卡的功能区多维表入口按钮使用DWB_TB-01-01.svg这个图标