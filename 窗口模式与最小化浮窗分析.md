# 多维表插件 — 窗口模式与「最小化浮窗」特性分析

> 适用版本：v2.9.42（2026-08-20）  
> 分析对象：当前主界面 `HtmlMainForm`（WebView2 单页应用宿主）及其在最小化时于屏幕左下角出现的小窗口。  
> 文档生成日期：2026-08-24

---

## 一、现象重述

用户操作：点击主界面窗口（标题为「多维表分析」）右上角的**最小化**按钮后，屏幕**左下角（状态栏上方）**出现一个**很小的窗口**，其右上角带有 **还原 / 最大化 / 关闭** 三个按钮；该小窗口**不会**悬浮在 Excel 窗口之上。

结论先行：**这个“小窗口”不是另一个独立控件，它就是主窗口 `HtmlMainForm` 自身处于最小化状态的“停靠残影”**。下文逐层解释成因、实现与结构。

---

## 二、这个“小窗口”的特性

| 特性 | 说明 |
|------|------|
| 本质 | 主窗口 `HtmlMainForm` 本身的最小化形态，并非新建的浮窗对象 |
| 出现位置 | 屏幕左下角、任务栏正上方 |
| 外观 | 仅剩标题栏尺寸（很小的条），带 还原 / 最大化 / 关闭 标准 Windows 标题按钮 |
| 与 Excel 的层级 | **默认不置顶**：不在 Excel 窗口之上，可任意被 Excel 或其它窗口遮挡 |
| 出现条件 | 由用户点击**本窗口自身的「最小化」**触发；与“Excel 最小化时本窗口跟随隐藏”是两条不同路径 |
| 可恢复 | 点击该小窗口即还原为主窗口 |

**为什么偏偏停在左下角？**
`HtmlMainForm` 是 `form.Show()` 弹出的**顶层窗口、且不以 Excel 为拥有者**（`RibbonController.OnOpenMultiTable` 中 `form.Show()`，无 owner 参数），同时设置了 `ShowInTaskbar = false`（不出现在任务栏）。

最小化一个「无任务栏按钮、无拥有窗口」的顶层窗口时，Windows 无处“收纳”它（没有任务栏缩略图可缩进），于是按传统行为把它**停靠（park）在屏幕左下角**，渲染成一个极小的图标化标题条——这就是你看到的小窗口。

**为什么它不在 Excel 上面？**
- `TopMost` 默认 `false`：仅在 `Load` 时短暂置 `true` 再立刻复位（仅保证首次打开能浮现），之后一直是普通层级（`HtmlMainForm.cs` 第 97–101 行）。
- 右上角“置顶”按钮走 `setTopMost`：即使开启，也通过**前台窗口钩子**限定「仅当 Excel（或本窗口）为前台时才 `TopMost=true`，切到其它程序立即取消」（`OnWinEvent` 的 `EVENT_SYSTEM_FOREGROUND` 分支，第 147–156 行；`SetTopMost` 第 876–894 行）。
- 因此正常状态下它就是一个普通 z-order 窗口，**不会**盖住 Excel。

---

## 三、实现方法（关键代码定位）

### 1. 窗口创建与弹出
- **入口**：Ribbon「打开多维表」按钮 → `RibbonController.OnOpenMultiTable`（第 139–158 行）。
  - 单实例保护：若 `HtmlMainForm.Instance` 已存在则 `Activate()`，否则 `new HtmlMainForm()` 后 **`form.Show()`（无 owner）**（第 156 行）。
  - 注释明确：*“不以 Excel 为‘拥有窗口’弹出，避免阻塞 Excel 任务栏的最小化/切换；‘随 Excel 最小化一起隐藏’由 WinEvent 钩子实现。”*
- **构造**：`HtmlMainForm()`（`HtmlMainForm.cs` 第 77–116 行）：
  - `Text = "多维表分析"`、`WindowState = Maximized`、`ShowInTaskbar = false`（第 81–87 行）；
  - `Icon = AppIcon.ico`（第 88 行）；
  - 内嵌 `WebView2` 控件 `Dock = Fill`（第 89–90 行）。

### 2. 跟随 Excel 最小化（隐藏/显示）
- `AttachExcelMinimizeFollow()`（第 124–142 行）：对 Excel 主窗口句柄挂 `SetWinEventHook(EVENT_SYSTEM_MINIMIZESTART, EVENT_SYSTEM_MINIMIZEEND, ... pid=Excel进程)`。
- `OnWinEvent`（第 144–166 行）：
  - `MINIMIZESTART`（Excel 最小化）→ `Visible = false`；
  - `MINIMIZEEND`（Excel 还原）→ `Visible = true`。
- **注意**：该钩子只监听 **Excel 句柄** 的最小化，不监听本窗口自身最小化。所以“你最小化 Excel → 本窗口跟随隐藏”与“你最小化本窗口 → 出现左下角小窗”是两套独立逻辑。

### 3. 置顶（仅对 Excel 有效）
- `AttachExcelForegroundWatch()`（第 172–185 行）：系统级 `EVENT_SYSTEM_FOREGROUND` 钩子。
- `OnWinEvent` 前台分支（第 147–156 行）：`_topMostWanted` 为真且当前前台是 Excel 进程时才 `TopMost = true`，否则取消。

### 4. UI 内容来源
- 全部界面（侧栏、视图区、弹窗、标题栏按钮）由内嵌 HTML/CSS/JS 单页应用绘制；
- `LoadEmbeddedHtml()`（第 285–298 行）从资源 `MultiTableAddin.Html.index` 读取，经 `NavigateToString` 注入；
- 窗口本身的「还原/最大化/关闭」则是 **WinForms 原生标题栏**（因 `Form` 默认带 `MinimizeBox`/`MaximizeBox`），不是 HTML 画的。

---

## 四、结构总览

```
Ribbon（Excel/WPS 功能区）
   └─ OnOpenMultiTable()
         └─ new HtmlMainForm().Show()        ← 顶层窗口，无 owner，ShowInTaskbar=false
               ├─ WebView2 (Dock=Fill)       ← 内嵌 index.html SPA（所有界面）
               ├─ WinEvent 钩子①：Excel 最小化 → 本窗口 Visible=false/true
               └─ WinEvent 钩子②：前台切换 → 置顶仅对 Excel 生效

最小化“本窗口自身” → Windows 因无任务栏/无 owner，停靠为左下角小窗（即本窗口最小化态）
```

**职责划分**：C#（`HtmlMainForm` + `RibbonController`）只负责「窗口宿主 + Excel 读写桥 + 窗口层级控制」；业务 UI 完全在 HTML/JS 里。这也是为什么改界面只需改 `index.html`、无需动 C# 窗口框架。

---

## 五、其它窗口模式介绍

本项目历史与现状中存在/曾存在以下几种窗口模式，可互为替代或互补：

### 模式 A：顶层浮动窗口（当前主模式，即上述 `HtmlMainForm`）
- **形态**：独立顶层 `Form`，最大化铺满，内嵌 WebView2。
- **特点**：不占任务栏、不阻塞 Excel 的切换/最小化；通过 WinEvent 钩子“跟随 Excel 最小化”。
- **适用**：作为独立工作台的主界面。**这就是你正在用的模式。**

### 模式 B：Custom Task Pane（CTP，Excel 内嵌停靠窗格）
- **入口**：`RibbonController.OnShowPane`（原生 WPF 视图）/ `OnShowWebPane`（WebView2 视图）→ `TaskPaneManager.Show` / `WebTaskPaneManager.Show`。
- **实现**：`CustomTaskPaneFactory.CreateCustomTaskPane(...)`，`DockPosition = msoCTPDockPositionRight`（**停靠在 Excel 右侧**，禁止横向停靠），按「活动窗口」分别绑定（`TaskPaneHostControl.cs` 第 264–284 行）。
- **特点**：**由 Excel 拥有**，随 Excel 一起最小化/还原、始终贴在 Excel 内；不存在“左下角小窗”问题。
- **适用**：希望界面“长”在 Excel 里的场景；目前更像可切换的备选形态。

### 模式 C：WPF 承载对话框（`FormBlankForWpf` + `DemoWindowService`）
- **形态**：一个 WinForms 空白宿主 `FormBlankForWpf` 内嵌 WPF `UserControl`。
- **两种弹出**：
  - 模态：`ShowHostedWpfDialog` → `form.ShowDialog(owner)` —— **阻塞 Excel**，关闭前不能操作表格（高版本运行时焦点不稳时的兜底）。
  - 非模态：`ShowHostedWpfControl` → `form.Show(owner)` —— 浮动但**以 Excel 为拥有者**（`TryCreateExcelOwnerWindow`），故始终在 Excel 之上、随 Excel 最小化。
- **适用**：会员中心、任务配置、批量导入等独立业务窗口（`PopupDemoView` 等模板）。

### 模式 D：标准模态对话框
- `MessageBox.Show`、`SaveFileDialog`、`FolderBrowserDialog` 等，父窗口设为 `HtmlMainForm`，用于警告、保存图片、选图片文件夹等（见 `HtmlMainForm.cs` 第 499–509、653–660 行）。

### 模式 E（历史/已废弃）：原生 WPF 多视图
- 早期架构用 `MultiTableMainView`、`KanbanView`、`GalleryView`、`GanttView` 等 WPF `UserControl` 直接承载各视图；现已被 WebView2 SPA 取代，多为死代码，仅 CTP 演示与个别对话框仍引用 WPF。

| 模式 | 拥有者 | 是否在 Excel 上 | 最小化归宿 | 典型用途 |
|------|--------|----------------|------------|----------|
| A 顶层浮动窗 | 无 | 否（默认） | 左下角小窗 | 主工作台 |
| B CTP 停靠 | Excel | 是（嵌在 Excel 内） | 随 Excel | 内嵌面板 |
| C WPF 对话框 | Excel（owner） | 是 | 随 Excel | 独立业务窗 |
| D 标准对话框 | 本窗口 | 是（模态） | 随父窗 | 提示/保存 |

---

## 六、设计取舍与可选改进（非本次改动，仅分析）

**为什么选模式 A 而非 B**：模式 A 不“拥有”Excel，因此**不会阻塞 Excel 任务栏的最小化与 Alt+Tab 切换**——这是当初刻意的选择（见 `RibbonController.cs` 第 154–156 行注释）。代价就是“本窗口自身最小化”时没有任务栏可收纳，表现为左下角小窗。这是一个**权衡后的副作用**，不是 bug。

若希望消除“左下角小窗”、又保留不阻塞 Excel 的优点，可选方向（均未实现，供参考）：
1. **自最小化也隐藏**：在 `OnWinEvent` 旁增加对本窗口自身 `EVENT_SYSTEM_MINIMIZESTART`（hwnd 为本窗口）的监听，最小化时直接 `Visible=false` 并从任务栏/屏幕完全收起，配合 Ribbon 按钮恢复；代价是用户失去“随时点小窗还原”的入口。
2. **改用 CTP 模式 B**：让界面真正嵌进 Excel 右侧，彻底规避浮窗问题，但会牺牲“独立大窗工作台”的体验、且切换表时需注意 CTP 按窗口绑定。
3. **`ShowInTaskbar = true` + 单实例**：让本窗口正常进任务栏，最小化即收进任务栏（最贴近普通软件的预期），但会与 Excel 各占一个任务栏按钮。

> 当前实现选择的是「独立浮窗 + 跟随 Excel 最小化」，左下角小窗是其已知表现。是否调整取决于你更看重“不阻塞 Excel 切换”还是“最小化体验更常规”。
