# 多维表分析插件 — 更新记录（2026-09-01 补丁）

> 适用版本基线：v2.9.42（StablePath · 2026-08-20）
> 补丁日期：2026-09-01
> 补丁内容：功能区按钮更名 + 甘特月/年视图右侧对齐 BUG 修复 + 功能图标统一强调蓝

## 一、变更概述

| 项目 | 修改前 | 修改后 | 类型 |
| --- | --- | --- | --- |
| Excel 功能区按钮文字 | 多维表 | 多维视图 | 文案调整 |
| 甘特视图（月/年）右侧对齐 | 图表左对齐、右侧留白 | 撑满可视区右缘、无空白 | BUG 修复 |
| 功能区/工具栏图标颜色 | 近黑色 | 统一强调蓝 `#3370FF` | 视觉修复 |

## 二、功能区按钮更名：多维表 → 多维视图

**位置**：`MultiTableAddin/src/MultiTableAddin/Resources/Ribbon.xml` 第 6 行（`button id="btnOpenMultiTable"`）。

```xml
<!-- 修改前 -->
<button id="btnOpenMultiTable" label="多维表" size="large" tag="RibbonOpen" getImage="GetButtonImage" onAction="OnOpenMultiTable" />
<!-- 修改后 -->
<button id="btnOpenMultiTable" label="多维视图" size="large" tag="RibbonOpen" getImage="GetButtonImage" onAction="OnOpenMultiTable" />
```

说明：功能区**选项卡**（tab `多维表`）与**分组**（group `多维表分析`）名称保持不变，仅「打开」按钮文字由「多维表」改为「多维视图」，更贴合插件「多视图浏览/编辑」的定位。`RibbonOpen.png` 图标本身为强调蓝，无需改动。

## 三、BUG 修复：甘特视图月/年模式右侧空白未对齐

### 现象
在甘特视图中切换为「月」或「年」时间刻度时，右侧甘特图轨道区没有对齐窗口右缘，留有一段空白；切换为「日」「季度」时显示正常。

### 根因（`index.html`）
甘特轨道区宽度由固定像素值决定：

```js
// index.html:2972
var GANTT_DAY_PX = {day:80, week:40, month:16, quarter:12, year:4}[dim] || 12;
// index.html:2973
var canvasW = Math.round(totalDays * GANTT_DAY_PX);  // 右侧轨道区像素宽度
```

- 月/年视图每「天」像素最少（月=16、年=4），当任务日期区间不大时，`canvasW` 计算出的像素宽度**小于右侧滚动区可视宽度**，图表左对齐后右侧即出现空白。
- 日（80）/季（12）每「天」像素大、总宽度溢出，自然填满可视区，因此表现正常。

### 修复（index.html:2974）
将轨道区宽度策略由「固定像素」改为「固定像素与 100% 取大」：

```js
// index.html:2974  #531 月/年视图 px/天较小，canvasW 可能窄于可视区，用 max 保证右对齐无空白
function setCanvasWidth(el){ el.style.width = 'max(' + canvasW + 'px, 100%)'; }
```

- 当 `canvasW` 比可视区窄时，撑满 `100%`，消除右侧空白；
- 当 `canvasW` 比可视区宽（日/季等）时维持 `canvasW`，仍可横向滚动。
- 甘特条、网格线、今天线均以图表**实际渲染宽度**为基准做百分比定位，两种情形下映射一致，条位置不受影响。

### 验证
重新构建后，月/年视图轨道区右缘与窗口右缘对齐，无空白；日/季视图仍可正常横向滚动。

## 四、图标配色：统一强调蓝

### 现象
功能区/主工具栏的图标（设置、刷新、帮助、复制全部）显示为近黑色，与视图切换侧栏的强调蓝不一致。

### 根因（`index.html`）
这些按钮使用 `className='btn sm'` + `<span class="ic">` + SVG（`stroke="currentColor"`），图标颜色随容器的 `color` 变化。基础规则 `.btn .ic`（原定义）**未设置 color**，图标继承 `.btn` 的正文色（浅色主题下近黑）。v2.9.42 仅给侧栏（`.sidebar-top .btn .ic`、`.view-item .ic`）上了蓝，主工具条漏改。

### 修复（index.html:24、26）
```css
/* index.html:24 */
.btn .ic{ display:inline-flex; align-items:center; justify-content:center; width:14px; height:14px; color:var(--accent); }
/* index.html:25-26 主按钮/激活态为蓝底白字，图标改白色避免同色不可见 */
.btn.primary .ic, .btn.active .ic{ color:#fff; }
```

- 设置 / 刷新 / 帮助 / 复制全部 图标统一为强调蓝 `#3370FF`；
- 复制全部位于主按钮（蓝底）上，其图标改用白色以保证可见。
- Excel 功能区「多维视图」按钮的 `RibbonOpen.png` 本身已是 `#3370FF`，无需改动。

### 验证
重新构建后，主工具栏图标呈统一强调蓝；蓝底主按钮上的图标为白色，对比清晰。

## 五、构建与校验

- `dotnet build -c Release`：**0 错误**（5 条历史 warning 均为死代码/可空解引用，未改动）。
- DLL 已复制到 `MultiTableAddin/dist/files/MultiTableAddin.dll`（876544 字节）。
- `python 脚本与工具/verify-build.py`：**EXIT=0，全部检查 OK**（功能区标签守卫 `label="多维表"` 仍由选项卡命中，保持通过）。

## 六、升级说明

本补丁直接覆盖 v2.9.42 的 `dist/files/MultiTableAddin.dll` 即可生效；旧配置（`.multiview.json` / 嵌入配置）向后兼容，无需迁移。
