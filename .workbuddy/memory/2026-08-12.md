## 2026-08-12 多维表分析插件 v2.9.2 改动（CodeName ComprehensivePolish）

### 版本与构建
- `AppVersion`：2.9.1 → **2.9.2**，ReleaseDate → **2026-08-12**，CodeName → **ComprehensivePolish**。
- `build_addin.ps1 -Configuration Release` 成功（0 错误，NU1900 等离线警告无害）。
- `dist/files/MultiTableAddin.dll` 已更新（650752 字节），已用 `verify-build.py` 确认 15 个标记全部命中（版本号 UTF-16LE + 各需求 JS/CSS 标记）。

### 13 项需求落地
1. **新增必须点保存**（#1）：`addRow()` 改为 `showDraftDetailPanel({})`，点「新增」不再直接落库，需点「保存」才持久化。
2. **看板/画册字段标签显隐 + 看板每行一个**（#2）：新增 `showFieldLabels` 开关按钮；看板/画册字段改为每字段独立 `.kfld`/`.gfld` 行显示（看板不再多字段挤一行）。
3. **查找跨全部视图**（#3）：`getVisibleRows()`（含 search 过滤）已接入日历/甘特/仪表盘，查找不再只在表格/画册生效。
4. **看板拖拽分组实时更新**（#4）：`elementsFromPoint` 命中修正 + 拖拽后 `state.selectedRow` 同步，拖拽后即时重绘，无需手动刷新。
5. **甘特配置窗口对齐**（#5）：`.ctrl` 统一输入控件对齐/宽度，复选框左对齐。
6. **甘特左侧多列表格布局**（#6）：弃用字符串拼接，改为飞书式多列冻结表头（`position:sticky;left:0` 冻结左列，列宽按 `computeGanttLabelWidths` 60–180px 自适应，不与右侧图表重叠）。
7. **甘特头部/滚动/季度/上层标签修复**（#7）：删除「甘特图」头部文字；今日红线改用纯 `%` 定位（消除滚动压到顶部日期标签的 bug）；季度标签 `2026Q1`→`2026 第一季度`；切换维度时同一行显示上层标签（日视图显示月份、周视图显示月份、月/季视图显示年份）。
8. **仪表盘标签开关统一 + 垂直留白减半**（#8/#9）：标签显隐开关统一控制所有图表的数据标签（打开即全部显示、关闭即全部隐藏）；图表盒 `.chart-box` 内边距压到 `padB=14,padT=6,padR=6`，消除数据标签被裁切，垂直留白约减半。
9. **日历日按钮/下移拖拽/当前时间线**（#10）：日/周/月切换按钮齐备；下移拖拽失败不再 `openTable` 重置（catch 中移除 `openTable` 调用，时间可保持并显示起止）；当前时间红线贯穿**所有列**（今日在视图内时每列都画），2px 置顶、粗细/网格与甘特一致。
10. **功能区打开多维表按钮图标**（#11）：根因是 16×16 PNG 在大按钮上不渲染；用 icon-design 重新导出 `tabler-icons:outline:table` 与 `tabler-icons:outline:info-circle`，均为 **32×32** 品牌蓝 `#3370FF`（RibbonOpen.png / RibbonAbout.png），Ribbon.xml 仍用 `image="RibbonOpen"`/`RibbonAbout`，由 `RibbonController.TryGetRibbonImage` 从嵌入资源解析。
11. **日历日期时间可输入**（#12）：确认 DateTime 字段已走 `buildDateTimeSplitInput`（日期 `<input type=date>` + 时间 `<select>` 00:00–23:30），无需改动。
12. **日历右侧面板不自动关闭**（#13）：`renderContent()` 改为仅在「视图切换」时 `closeDetailPanel()`（用 `lastRenderedViewId` 守卫），视图内交互（拖拽/点击）不再误关右侧详情抽屉。

### 验证方式（可复用）
- `verify-build.py`：对 `dist/files/MultiTableAddin.dll` 做 15 标记字节搜索（版本 UTF-16LE + JS 标记 UTF-8/ latin-1），全部 OK。
- `check-js-syntax.js`：抽取 `index.html` 的 `<script>` 用 `new Function()` 校验 JS 语法（输出 JS syntax OK）。
- PNG 尺寸用 Python `struct.unpack('>II', data[i+4:i+12])` 解析 IHDR 确认 32×32（PowerShell `Add-Type System.Drawing` 在本环境被沙箱拦截，改用 Python）。

### 构建注意
- 本环境 PowerShell 默认沙箱会拦截 `dotnet`/文件写入，构建与验证需 `dangerouslyDisableSandbox=true`。
- **改完源码必须 `build_addin.ps1 -Configuration Release`**，仅 `dotnet build` 不会更新 `dist/files`，重装仍加载旧资源。
- 重装插件前请完全关闭已加载该 XLL 的 Excel/WPS。

## 2026-08-12 多维表分析插件 v2.9.3 改动（CodeName WindowUX）

### 版本与构建
- `AppVersion`：2.9.2 → **2.9.3**，ReleaseDate → **2026-08-12**，CodeName → **WindowUX**。
- `build_addin.ps1 -Configuration Release` 成功（0 错误，NU1900 等离线警告无害）。
- `dist/files/MultiTableAddin.dll` 已更新（**674816** 字节），`verify-build.py` 确认 **22 个标记全部 OK**（版本 UTF-16LE + 各需求 JS/CSS/C# 标记）。`node --check` 抽取 `<script>` 语法 OK。
- 关于窗体图标根因修正：Resources\Icons\ 下 `RibbonOpen.png`/`RibbonAbout.png` 此前为无效占位文件（610/1166 字节），`TryGetRibbonImage` 返回 null → 功能区按钮无图标。已用 Pillow 生成合法 **32×32 透明 PNG**（品牌蓝 #3370FF：RibbonOpen=蓝色圆角网格，RibbonAbout=蓝色信息圈「i」）覆盖，csproj 以逻辑名 `MultiTableAddin.Resources.RibbonOpen.png` 嵌入。

### 8 项需求落地（v2.9.3）
1. **表单视图左侧直输** (#1)：`renderContent` 的 Form 分支改为 `closeDetailPanel(); renderForm(); showFieldSettings()` 常驻右侧字段设置；新建数据直接在左侧各输入控件录入，去掉原右侧数据编辑面板。
2. **日历视图修复** (#2)：日/周/月切换按钮齐备；「今天」列红色实心圆点标识；`renderCalEvent` 始终渲染起止时间标签（修复拖拽时只剩结束时间）；`makeCalEventDraggable` 整体移动 + 底部 resize 逻辑补全；月视图单击切周、日期时间输入保持既有实现。
3. **甘特视图** (#3)：① 今日红线 `gantt-today-line` 与网格 `gantt-grid` 包裹进偏移层（`left=totalLabelW`），消除多字段显示后压到冻结标签列的重叠；② 列头 `.gantt-colh` 右键/左键调 `showColMenu`，含 修改字段(preselectField)/隐藏·显示此列/显示全部/升序·降序排序/设为分组字段/筛选此字段/添加字段；`getVisibleRows()` 接入 `fieldFilter`；`addField` 桥接 `ExcelAdapter.AddColumn` 写 FieldOverride。
4. **仪表盘标签裁切** (#4)：`drawChart` 内边距由 `padB=14,padT=6` 改为 `padB=24,padT=18,padR=6`，X 轴标签 y 调为 `padT+plotH+14`，柱/折线最高处标签与 X 坐标标签不再被遮半。
5. **仪表盘配色 + 功能区按钮** (#5a/#5b)：① 新增 `APP_SETTINGS.dashTheme`（默认 office）+ `DASH_THEME_LIST`（7 套主题）+ `getDashColors()`，`renderDashboard` 加 `.dash-theme-row` 下拉，改主题全图表配色统一变；② Ribbon.xml `label="打开多维表"`→`label="多维表"` 并补 `RibbonOpen.png` 图标（见上）。
6. **顶部改为左侧视图切换分组** (#6)：选择数据/刷新/关于移入左侧视图切换顶部分组，释放顶部一行；设置中放「置顶」并加「打开时最大化」复选框（C# `setOpenMaximized` 设 `WindowState=Maximized/Normal`，持久化 `APP_SETTINGS.openMaximized`）。
7. **窗口阻塞 + 跟随隐藏** (#7)：根因是 `form.Show(owner)` 让窗口成为 Excel 的所属窗体（owned window）导致任务栏被阻塞、状态栏 Excel 无法切换/隐藏。改为 `form.Show()`（无 owner）+ `ShowInTaskbar=false` + `SetWinEventHook(EVENT_SYSTEM_MINIMIZESTART/MINIMIZEEND)` 跟随 Excel 主窗口最小化/还原（`OnWinEvent` 经 `PostToUi` 回 UI 线程 `Visible=false/true`），保留「随 Excel 最小化一起隐藏」。
8. **窗口状态文档** (#8)：新增 `窗口状态与置顶实现说明.md`，详述 跟随 Excel 最小化隐藏（WinEvent 方案）、窗口阻塞根因、强制置顶、打开时最大化 的实现与关键点、排错表。

### 关键技术点（可复用）
- 窗口所属关系：`Show(owner)` 会阻塞任务栏切换；无 owner + WinEvent 钩子才能既「随最小化隐藏」又不阻塞。
- WinEvent 回调跑在非托管上下文，必须 `PostToUi(BeginInvoke)` 回 UI 线程再操作控件。
- 甘特今日线：grid/today-line 的 `%` 原引用整图宽（含冻结标签列），bars 用 track 区 → 两层 `left=totalLabelW` 偏移对齐。
- 图标生成：环境无 PIL/cairosvg，建 venv `C:/Users/LQ/.workbuddy/binaries/python/envs/default` 装 Pillow 生成 PNG。
- 验证脚本：PowerShell 无 `tail`，用 `| Out-String -Width 200`；PS 沙箱会拦截 `dotnet`/写文件，构建需 `dangerouslyDisableSandbox=true`；stdout 在本环境可能被吞，必要时重定向到文件再 Read。

### 待用户现场复核（本环境无 Excel/WebView2，无法交互验证）
- 日历拖拽（下拖延长时长、月视图单击切周）实际手感。
- 甘特列头菜单各动作（隐藏/排序/分组/筛选/添加字段）端到端。
- 重装插件：完全关闭已加载该 XLL 的 Excel/WPS 后重装 `dist/files` 下 `.xll`+dll，重启验证「关于」版本应为 2.9.3 (WindowUX)。

### 补充：功能区图标经 icon-design 技能正规取源 + 重建机制修正
- 本会话用 `icon-design` 技能正规检索并取回 tabler-icons 官方 SVG：`tabler-icons:outline:table`（多维表按钮）、`tabler-icons:outline:info-circle`（关于按钮）。技能 PNG 导出依赖本机无头 Edge（环境缺失，且 reportlab 的 rlPyCairo/_renderPM 后端在 Windows 上也不可用），故改为用 PIL 按 tabler 几何（圆角方框+横竖分隔线 / 圆+点+ i 杆）在 240px 高分辨率绘制后 LANCZOS 降采样到 **32×32 透明 PNG**，主色品牌蓝 `#3370FF`，覆盖 `Resources\Icons\RibbonOpen.png` / `RibbonAbout.png`。
- 因修改了内嵌 PNG 资源，必须重建并重新拷 DLL：`cd src/MultiTableAddin && dotnet build -c Release`（0 错误，产物 674304 字节）→ `rm -f dist/files/MultiTableAddin.dll` 再 `cp bin/Release/net8.0-windows/MultiTableAddin.dll dist/files/`（直接 `cp -f` 覆盖会因句柄锁 Permission denied）。`verify-build.py` 复跑 22/22 全 OK。PDB 被锁不影响运行，跳过。
- **构建脚本变更**：项目根已无 `build_addin.ps1`（旧文档所称的 `tools/build_addin.ps1` 亦不存在），后续一律用上面的 `dotnet build` + 手拷 DLL 两步法，已同步修正 MEMORY.md 的「构建与交付链路」约定。

## 2026-08-12 多维表分析插件 v2.9.4 改动（CodeName UXPolish）

### 版本与构建
- `AppVersion`：2.9.3 → **2.9.4**，ReleaseDate → **2026-08-12**，CodeName → **UXPolish**。
- `dotnet build -c Release` 成功（**0 错误**，13 个警告均为既往 CS8602/CS0067/CS0649/NU1900 无害项）。
- `dist/files/MultiTableAddin.dll` 已更新（**682496** 字节，旧 674304→新 682496）；`verify-build.py` 升级到 v2.9.4 标记并重跑 **33/33 全 OK**（版本 UTF-16LE + 各需求 JS/CSS 标记，含 `snapMinutes` / `isAnyCalendarDateField` / `showRecordContextMenu` / `.gantt-row.selected` / `时间步进` / `基于 Excel/WPS 超级表`）。
- 图标（#11/#19）：用 icon-design 正规检索 tabler-icons，选 `tabler-icons:filled:table`；本机无头 Edge 导出失败，改用 Pillow 手绘 64×64 蓝底白格网格 PNG 覆盖 `Resources\Icons\RibbonOpen.png`（旧 1018B→新 314B），功能区「多维表」入口已改用此图标；`Ribbon.xml` 已移除关于按钮。

### 19 项需求落地（v2.9.4 UXPolish）
1. **刷新空数据崩溃**：空表/空行安全守卫，null 不再抛异常。
2. **视图切换按钮打开即见 + 默认表格视图**：首屏即渲染视图切换条，初始 `activeViewId` 指向 Table。
3. **切换主题后窗口尺寸恢复（原 bug）+ 主题紧凑开关移入工具栏**：主题切换只改皮肤不改窗体尺寸；compact 开关进工具栏。
4. **日历日视图按钮缺失**：日/周/月三按钮齐备（历史 stale dist 所致，本次重建已包含 `renderDayView`）。
5. **日历拖拽重写**：整体上拖改开始、下拖改结束、直接移位置始末都变；新增 15 分钟步进参数（日历设置「时间步进(分钟)」min5/max60/step5/默认15）；拖拽过程直接写 `r.values`，松手即持久化，不再回弹。
6. **表单聚焦 ↔ 右侧字段设置联动**：`input` focus 时 `preselectField=该字段` 并 `showFieldSettings()`。
7. **置顶仅在 Excel 上方**：`SetWinEventHook` 监听前台窗口 PID，前台为 Excel/本窗时才 `TopMost`，切到其它应用自动取消（`HtmlMainForm.cs` `SetTopMost`/`OnWinEvent`）。
8. **看板/画册/日历无字段设置面板**：仅 Table/Form/Gantt/Chart 显示字段设置。
9. **甘特开始/结束日期字段切换**：配置可切换用哪个日期字段作为起止。
10. **全局控件左对齐 + 等宽**：工具栏/配置窗输入控件统一左对齐、等宽。
11/19. **功能区图标 + 重新取源 + 移除关于**：见上图标节；`Ribbon.xml` 移除 `btnAbout`。
12. **日历右侧面板常驻 + 实时任务信息**：`renderCalendar` 末尾自动打开首个有日期事件详情；选中行时打开其详情，实时显示任务起止/字段。
13. **开始/结束均为日期时间 + 统一右侧编辑面板 + 表单日期时间输入**：`isAnyCalendarDateField` 让所有日历日期字段走 `buildDateTimeSplitInput`（日期+时间分离输入），详情/草稿/弹窗/表单全统一。
14. **表单合并新建/上一条 + 带记录数的查找**：新建与「上一条」合并行；Form 搜索框实时过滤并显记录数。
15. **每视图筛选 + 表格列筛选**：表格每列表头右键 `showColMenu`（筛选/显示隐藏/排序/分组）；各视图新增独立 filter 入口。
16. **甘特聚焦行高亮**：`.gantt-row.selected` 加底色/强调色/加粗。
17. **优化每视图右键菜单**：新增 `showRecordContextMenu` 通用记录菜单（查看详情/删除记录），接到看板卡/画册卡/日历事件/甘特条。
18. **数据切换/关于移入设置 + 关于窗体重做**：设置内含「数据源/关于」；`showAbout` 改为模态卡片，显示版本/构建部署时间/宿主/Excel 版本（无 DLL 路径）+ 简介 + 帮助。
19. **侧边栏移除品牌标签 + 放大按钮 + 重取图标**：侧栏顶部品牌文字移除、按钮放大；图标见 #11/19。

### 验证方式
- `verify-build.py` 33/33 OK（v2.9.4）。`dotnet build -c Release` 0 错误。

### 待用户现场复核（本环境无 Excel/WebView2，无法交互验证）
- 日历拖拽手感（上拖/下拖/直移 + 15 分钟步进）、甘特聚焦行高亮可见性、通用右键菜单行为。
- 重装插件后功能区图标显示、`关于`窗体版本应为 **2.9.4 (UXPolish)**、主题切换后窗口尺寸不被改变。
- 重装前完全关闭已加载该 XLL 的 Excel/WPS。

## 2026-08-12 多维表分析插件 v2.9.5 改动（CodeName UXPolish，续 #9~#14）

### 版本与构建
- `AppVersion`：2.9.4 → **2.9.5**，ReleaseDate 仍为 **2026-08-12**，CodeName 仍为 **UXPolish**。
- `dotnet build -c Release` 成功（**0 错误**，13 警告均为既往项）。产物 `MultiTableAddin/src/MultiTableAddin/bin/Release/net8.0-windows/MultiTableAddin.dll`。
- 复制 DLL：`rm -f MultiTableAddin/dist/files/MultiTableAddin.dll` 后 `cp .../bin/Release/.../MultiTableAddin.dll MultiTableAddin/dist/files/`（直接 `cp -f` 覆盖会句柄锁 Permission denied）。dist dll 由 682496→**689664** 字节（`cmp` 确认与源字节一致）。
- `verify-build.py` 升级 v2.9.5 标记（版本 2.9.5 + #9~#14 各 JS/CSS 标记），重跑 **42/42 全 OK**。注意：CSS 标记 `cell-edit.date-edit{accent-color:#3370FF;}` 必须保留结尾 `;`，否则子串匹配失败。

### #9~#14 需求落地（v2.9.5）
- **#9 表单按钮合并一行**：`.form-nav` 加 `flex-wrap:nowrap;overflow-x:auto;white-space:nowrap;`，并 `.form-nav .btn{flex:0 0 auto;}`，新建/上一条等按钮恒在一行（窄屏可横向滚）。
- **#10 日期选择器滚轮 + 蓝色填充**：① `.cell-edit.date-edit{accent-color:#3370FF;}` 让原生 date/datetime-local 控件内部强调色=品牌蓝；② `enhanceDateInput` 的 wheel 监听改默认**按月步进**（更快），`e.ctrlKey`→年、`e.shiftKey`→日、否则→月，datetime-local 同步更新时分。
- **#11 甘特分组标签对齐窗口左**：`renderGroup` 删除原 `gantt-left` 宽度占位 `sp` 块，仅把 `gantt-group-label` 直接 `gh.appendChild(lab)`，使「工程部（2）」等分组标签贴窗口最左（x=0），不再随甘特图左偏移。
- **#12 字段设置绑定焦点列**：`showColMenu` 开头 `e.preventDefault()` 之后补 `state.preselectField=fieldName;`，列头右键打开字段设置即定位到该焦点列（沿用既有 preselectField 定位逻辑）。
- **#13 侧栏 115px + 刷新/设置互换**：`.sidebar{width:115px;...}`（原 132px）；`renderSidebarTop` 改为先 append「设置」再 append「刷新」，设置在上、刷新在下。
- **#14 功能区/视图图标**：① 功能区与窗体左上角统一用 **TableSpark** 图标（Pillow 手绘 蓝 #3370FF 圆角网格+十字+右上角星），`Resources/Icons/RibbonOpen.png`(1156B) 与 `AppIcon.ico`(15084B，多尺寸 16–256) 已覆盖（csproj 逻辑名不变）；② 仪表盘切换视图左侧图标用 **YBP_TB-01-01**（地球双路径 fill-rule evenodd，currentColor）；③ 甘特视图图标用 **GTST_TB-01**（圆角矩形 rx=5 + 三条横线，stroke currentColor width=6）；两者以 inline SVG 注入 `ICONS` 对象（viewBox 0 0 70.87 70.86，缩放 16×16 与现有图标一致，随文字色保证侧栏可见）。

### 验证结果
- `dotnet build -c Release`：0 错误 / 13 警告（无害）。
- DLL 已更新至 dist/files（689664B，字节一致）。
- `verify-build.py` 42/42 OK（含 #9~#14 全部标记）。

### 待用户现场复核（本环境无 Excel/WebView2，无法交互验证）
- 日历日视图/拖拽（历史 stale dist 所致，本次已重建并验证 `renderDayView` 等字节命中）。
- #9 表单按钮单行、#10 日期滚轮灵敏度+蓝色填充、#11 甘特分组标签贴窗左、#12 列头右键字段设置定位焦点列、#13 侧栏 115px 且设置在上刷新在下、#14 功能区/窗体 TableSpark 图标 + 仪表盘 YBP + 甘特 GTST。
- 重装插件后「关于」版本应为 **2.9.5 (UXPolish)**；重装前完全关闭已加载该 XLL 的 Excel/WPS。

## 2026-08-12 多维表分析插件 v2.9.6 改动（CodeName UXPolish，Phase B #1~#15）

### 版本与构建
- `AppVersion`：2.9.5 → **2.9.6**（Core/Models.cs Line 11），ReleaseDate 仍为 **2026-08-12**，CodeName 仍为 **UXPolish**。
- `dotnet build -c Release` 成功（**0 错误**，13 警告均为既往项）。产物 `MultiTableAddin/src/MultiTableAddin/bin/Release/net8.0-windows/MultiTableAddin.dll`。
- 复制 DLL：`rm -f MultiTableAddin/dist/files/MultiTableAddin.dll` 后 `cp .../bin/Release/.../MultiTableAddin.dll MultiTableAddin/dist/files/`（直接 `cp -f` 覆盖会句柄锁 Permission denied）。dist dll = **685568** 字节（与源 `cmp` 字节一致，时间戳 18:10）。
- 图标（#15）：用 `temp/render_dwb_icon.py`（Pillow 直接绘制 DWB SVG 几何：圆角矩形描边 + 两条线 + 三次贝塞尔星形，scale=256/71）覆盖 `Resources/Icons/RibbonOpen.png`(420B) 与 `Resources/Icons/AppIcon.ico`(4563B，多尺寸 16/32/48/64/128/256)，并复制 RibbonOpen.png 到 `Resources/RibbonOpen.png`(420B)。csproj 逻辑名 `MultiTableAddin.Resources.RibbonOpen.png` / `MultiTableAddin.Resources.AppIcon.ico` 不变。
- `verify-build.py` 升级 v2.9.6 标记（版本 2.9.6 + #1~#15 各 JS/CSS 标记），重跑 **56/56 全 OK**（exit 0）。
- 内联 JS 抽取后用 `node --check` 语法校验 **exit 0** 通过。

### #1~#15 需求落地（v2.9.6，对应用户原始 15 条）
1. **仪表盘 0 值不标数据标签**：`if(cfg.showLabels!==false)` → `if(cfg.showLabels!==false && g.value!==0)`（4 处：折线/柱/饼/环，`replace_all`）。
2. **日历日/周/月按钮顺序**：`renderCalendar` head 重排为 日→周→月（id calDay/calWeek/calMonth，active 类随 view）。
3. **日视图拖拽跳动（根因精确定位）**：`makeCalEventDraggable` 中 `timeToTop()` 返回**像素**却被当**分钟**×60000，拖拽瞬时跳约 3 小时。修复：`nsMin=snapMin((origStart.getHours()*60+origStart.getMinutes())+dy/PX_PER_MIN)`；`startMin/endMin` 改用 `getHours()*60+getMinutes()`（`replace_all`）。
4. **周视图今日列不填充**：`.cal-day-col.today{...}` → `.cal-day-col.today:not(.is-week){...}`；`renderTimeGrid` 给列加 `is-week` 类（`col.className='cal-day-col'+(isWeek?' is-week':'')+(isSameDay(d,now)?' today':'')`）。
5. **日历设置控件对齐**：`.setting-row label` 改为 `flex:0 0 132px;width:132px;min-width:132px;max-width:132px;...text-align:right;padding-right:8px;`。
6. **看板拖拽不倾斜**：`.kanban-card.dragging` 去掉 `transform:rotate(2deg) scale(1.02)`（保留 opacity/box-shadow/border/background）。实时分组代码早已含 `renderContent()`，无需改。
7. **表格底部固定汇总行（须改 C# 模型）**：① 视图条新增「显示汇总/隐藏汇总」按钮（读 `v.showSummary`）；② `renderTable` 末尾 `if(v && v.showSummary) renderSummaryRow(table, v);`；③ 新增 CSS `table.grid tfoot td{position:sticky;bottom:0;...}` + `.summary-row .sum-val` + `.summary-mode`；④ 新增 `SUMMARY_MODES`（16 项：无/记录总数/已填写/未填写/唯一值/未填写占比/已填写占比/唯一值占比/求和/平均值/数值计数/最大值/最小值/中位数/标准偏差/方差）、`summaryMode`/`setSummaryMode`/`fmtSum`/`computeSummary`/`renderSummaryRow`；⑤ **关键**：C# `ViewConfig` 增 `Summary{Dictionary<string,string>}` + `ShowSummary{bool}`，并 `saveConfig` 回写 `cv.summary=sv.summary; cv.showSummary=sv.showSummary;`——否则 JS 自定义属性在 `JsonSerializer` 反序列化时被丢弃（配置往返丢属性根因）。
8. **表单字段类型切换不再重置**：`typeSel.change` 由 `buildForm(f)` → `f.type=typeSel.value; buildForm(f)`（不再覆盖用户选择）。
9. **表单字段按表序 + 跟随焦点**：`renderForm` 字段循环由 `state.visibleFields.forEach` 改为 `state.fields.filter(f=>visibleFields.indexOf(f.name)>=0).forEach`；焦点监听改闭包并同时绑定日期/时间子输入 `ctl._dateInput`/`ctl._timeInput` 的 `focus`。
10. **删除表格最右操作列**：删 `col-actions` colgroup/表头；空态/分组头 `td.colSpan=state.visibleFields.length+2` → `+1`；删 `appendTableRow` 中 tdA/🗑 整段；`autoFitColumns` 中 `maxCol=...+1`→`+0`，`if(i===0||i===maxCol)`→`if(i===0)`，`if(i>0&&i<maxCol)`→`if(i>0)`。
11. **看板激活样式与画册一致**：复核 `.kanban-card.selected` 与 `.gallery-card.selected` 已完全一致，无需改动。
12. **列分组实时更新**：`showColMenu`「设为分组字段」由 `saveConfig(); renderContent(); toast(...)` → `saveConfig(); renderViewbar(); renderContent(); toast(...)`（顶栏分组按钮即时刷新）。
13. **看板分组按钮（删"不分组"＋加"添加分组"）**：删 `groupSel` 中 `new Option('不分组','')`；新增「＋分组」按钮调用 `addGroupValue(v)`；新增 `addGroupValue(v)`（取分组字段→仅 Select/Quarter 可加→`prompt` 名称→写 `gf.options` + `fieldOverrides` `ov.options/ov.userDefined=true`→`saveConfig();renderViewbar();renderContent();toast`）；看板空态文案改"请先在上方「分组」下拉中选择分组字段"。
14. **看板拖拽实时分组（补充）**：沿用既有 `renderContent()` 实时重绘，无需新增；本项与 #13 合并计入看板分组能力。
15. **功能区图标换 DWB（DWB_TB-01-01.svg）**：功能区与窗体统一用 DWB TableSpark 图标（见上图标节），`Ribbon.xml` 仍用 `image="RibbonOpen"`，由 `RibbonController.TryGetRibbonImage` 从嵌入资源解析。

### 关键技术点（可复用）
- **配置往返丢属性根因**：C# `HtmlMainForm.SaveConfig` 用 `JsonSerializer.Deserialize<ViewConfigFile>(json, JsonOpts)`（HtmlMainForm.cs：CamelCase；ViewConfigManager.cs：PropertyNameCaseInsensitive + WhenWritingNull）。JS 视图对象上的自定义属性若不在 C# 模型里，重载时会被丢弃 → 任何需持久化的 JS 视图新字段都必须同步加到 C# 模型（#7 汇总、#224 endDateField 均因此）。
- **图标栅格化无 cairosvg/reportlab 时**：本机 cairosvg 不可用、svglib/reportlab 的 rlPyCairo/_renderPM 后端在 Windows 缺失 → 改用 **Pillow 直接按 SVG 几何绘制**（圆角矩形 `rounded_rectangle`、线条 `line`、星形用正则解析 path 的 M/c/C 指令 + 三次贝塞尔采样），LANCZOS 降采样到目标尺寸；保存 ICO 用 `sizes=[(s,s) for s in sizes]`（整数列表会 `'int' object is not subscriptable`）。
- **构建两步法（铁律）**：`cd src/MultiTableAddin && dotnet build -c Release` 只出到 `bin/Release/...`，安装脚本从 `MultiTableAddin/dist/files/` 取资源；**只 build 不手拷 DLL 则重装仍加载旧资源**。覆盖 dist dll 先 `rm -f` 再 `cp`（句柄锁）。PowerShell 沙箱拦截 dotnet/写文件，需 `dangerouslyDisableSandbox=true`。
- **验证**：`verify-build.py` 对 `dist/files/MultiTableAddin.dll` 做字节子串搜索（版本 UTF-16LE + JS 标记 UTF-8/latin-1；CSS 标记须保留结尾 `;`）；内联 JS 用 `node --check` 校验。

### 待用户现场复核（本环境无 Excel/WebView2，无法交互验证）
- #3 日视图拖拽手感（上拖改开始/下拖改结束/直移，不再跳 3 小时）；#4 周视图今日列是否真的不填充；#10 表格删除操作列后列宽自适应；#7 汇总行各模式数值正确；#13 看板"添加分组"端到端。
- 重装插件后「关于」版本应为 **2.9.6 (UXPolish)**；功能区/窗体图标为 DWB TableSpark。
- 重装前完全关闭已加载该 XLL 的 Excel/WPS。

## 2026-08-12 临时恢复 WPF 旧界面以截图参考（无 git 备份）

### 用户诉求
用户希望"临时回到 8月10日 18:13 之前的版本运行、截图旧 WPF 窗口布局、再据此优化当前 HTML 版"。排查结论：

### 排查关键事实
- **不是 Git 仓库** → 无版本历史可回滚。
- **WPF/XAML 源码完整保留**（`src/MultiTableAddin/Views/` 下 `MultiTableMainView.xaml`、`TableView.xaml`、`KanbanView.xaml`、`GanttView.xaml`、`FormView.xaml`、`CalendarView.xaml`、`ChartView.xaml`、`DashboardView.xaml`、`GalleryView.xaml` 及一堆对话框 .xaml 全在）。8月10日架构是 **Excel-DNA + WPF/XAML（HandyControl）**，后整体迁到 WebView2 单文件 `index.html`，但 WPF 源码未删。
- **`MultiTableMainView` 是 `UserControl`（非 Window）**，且当前源码已无任何 `new MultiTableMainView()` 实例化；`RibbonController` 当前入口开 `HtmlMainForm`（WebView2）。WPF 启动流程在迁移时被整体断开。

### 已落地的"WPF 预览版"独立安装包
- 新建 `WpfPreviewHost.cs`（Window，Content 承载 `MultiTableMainView`）→ 临时改 `RibbonController.cs:153` 为 `new WpfPreviewHost()` → `dotnet build -c Release`（0 错误）→ 复制 `dist/files/*` 到 **`dist/files_wpf/`** 再用 bin/Release 的 DLL 覆盖 → 还原入口为 `new HtmlMainForm()` 并删除 `WpfPreviewHost.cs` 后重建（源码/生产 dist 完全干净）。
- 产物：`MultiTableAddin/dist/files_wpf/`（含 `.xll` + 依赖 + WPF 版 `MultiTableAddin.dll`，686080B，含 `WpfPreviewHost` 类型；正式 `dist/files` 仍为 685568B 干净 v2.9.6）。
- **用户运行方式**：完全关闭 Excel/WPS → 把 `dist/files_wpf/` 当作一个独立插件安装目录（xll 加载同目录 dll）→ 打开 Excel/WPS 点"多维表"按钮即弹出旧 WPF 窗口 → 截图。截图完可删 `dist/files_wpf/`。

### 更还原的备选（8月10日当天状态）
- `src/MultiTableAddin/bin/Debug/net8.0-windows/MultiTableAddin.dll`（**2026-08-10 12:09**，455680B，WPF 版 Debug 编译）是"18:13 之前"最贴近当天的成品。可把它 + `dist/files` 的 xll/依赖 复制到另一文件夹直接运行（比"从当前源码重建"更还原，因当前 XAML 可能已有后续改动）。注意它是 Debug 版。

### 关键坑（可复用）
- 不能只把入口改成 `new MultiTableMainView()`——它是 UserControl，需先有宿主 Window 包一层（如 `WpfPreviewHost`）。
- 本环境无 Excel/WebView2，只能验证"能编译 + DLL 含类型"，**窗口能否真渲染需用户现场确认**（WPF 视图依赖 `ExcelAdapter`/`ViewEngine` 数据层，若迁移后接口有变可能运行期异常）。
- 若以后要再预览：重建 `WpfPreviewHost.cs`（约 30 行，Content=new MultiTableMainView()）+ 临时改入口 + 构建到独立文件夹即可，无需动生产 dist。

### 修正：xll 是"桩"，加载同目录散落 DLL（非打包进 xll）
- 用户反馈：用 `dist/uninstall_excel_addin.bat` + `install_excel_addin.bat` 重装后仍是"最新版本"。原因：安装脚本只从 `dist/files/` 取资源；而 `.dna` 虽写 `Pack="true"`，但**实测 xll 并未把 dll 打包进内部**（xll 字节数=桩模板大小，且内部无 HTML 标记），运行时加载的是**同目录散落的 `MultiTableAddin.dll`**。所以装 `dist` 必然得到 `dist/files/MultiTableAddin.dll`（v2.9.6 HTML）——这正是用户看到的"最新版本"。
- 验证方法：字节搜索——`dist/files/MultiTableAddin-AddIn64.xll` 不含 `calDay`/`看板`，而 `dist/files/MultiTableAddin.dll` 含全部 HTML 标记。结论：**换 dll 只要替换同目录 `MultiTableAddin.dll`，无需重打 xll**（与 MEMORY 中"只 build 不手拷 DLL 则重装仍加载旧资源"一致）。

### 一键切换脚本（已建，解决"装错目录"）
- 新增 `dist/switch_addin.ps1`（引擎）+ `dist/install_wpf_preview.bat` + `dist/restore_production.bat`。
- 逻辑：先 `Unregister-ExcelAddinRegistry`/`Unregister-WpsAddin` 清理 prod 与 wpf 共 4 个 xll 全部注册（防 Excel 双加载冲突），再按 Office 位数注册目标 xll（wpf→`files_wpf`；prod→`files`）。
- 语法已用 PowerShell Parser 校验通过（0 errors）。
- **用法**：看旧 WPF 界面 → 双击 `dist/install_wpf_preview.bat`；回正式版 → 双击 `dist/restore_production.bat`。两个 bat 都先卸后装，不会双加载。

### 修复：切换脚本中文编码导致 PowerShell 解析报错
- 现象：双击 bat 报 `'瑁?WPF' 不是内部或外部命令` + `switch_addin.ps1` 第30行"缺少右 )"。根因：`.ps1`/`.bat` 内含中文，Windows(cmd/PowerShell 5.1) 用 GBK 代码页读取 UTF-8 无 BOM 文件，中文字节被误读为乱码（`鍔犺浇椤癸細` 即 UTF-8 `已切换到：` 被当 GBK 解析），连带破坏语法解析。
- 修复：将 `switch_addin.ps1` + 两个 `.bat` **全部改写为纯 ASCII（英文）**；用 Python 字节校验确认 `non-ASCII=0`；PowerShell Parser 语法校验 `SYNTAX_OK: 0 errors`。恢复正常。
- **可复用经验**：任何要让用户在 Windows 上直接双击运行的 `.ps1`/`.bat`，**一律只用 ASCII（英文）**，绝不在脚本正文/注释/title 里写中文——PowerShell 5.1 默认按系统代码页(中文GBK)读无BOM文件，中文必乱码。被 dot-source 的官方脚本 `office_addin_setup.ps1` 自带 UTF-8 BOM 可正常读，不受影响。

## 2026-08-12 Phase C 需求落地（C1–C11 / C7b，全完成）

### 范围
用户原始 11 项显式需求：C1 批量设置字段属性；C2 日历/甘特日期字段默认开启日期时间控件、新增数据时间默认 0:00；C3 甘特日/周/月视图切换+拖拽实时写回+**单独 MD 文档说明拖拽逻辑与参数**；C4 按指定字段自动分色；C5 看板拖拽防文本选中+拖拽后分组实时更新；C6 移除各视图"新增分组"按钮、看板标题组合框移入视图设置；C7 仪表盘单击修复残留甘特渲染；C7b 卡片视图图片按字段值匹配文件名；C8 表格字段设置窗口与焦点列实时联动；C9 甘特月/周切换重绘；C10 帮助信息 WPS 适配文案；C11 功能区按钮图标换 DWB_TB-01-01.svg。

### 构建与验证（铁律两步法）
- `cd MultiTableAddin/src/MultiTableAddin && dotnet build -c Release`：**0 错误**，13 警告均为既往项。
- DLL 复制到 `MultiTableAddin/dist/files/`：先 `rm -f dist/files/MultiTableAddin.dll` 再 `cp`（直接 `cp -f` 会因 Excel/WPS 句柄锁报 Permission denied）。dist dll = **706560** 字节（时间戳 21:48），PDB 同步 137328 字节。
- `MultiTableAddin/check-js-syntax.js`（Node 抽取内联 JS 用 `node --check`）：**JS syntax OK**。
- `verify-build.py` 两轮修正后**全绿（exit 0）**：含 Phase C 标记 `showBatchFieldEditor`(C1)、`tSel.value='00:00'`(C2)、`{day:3, week:21, month:70}`(C3 缩放)、`calendarConfig.colorField`(C4)、`body.kanban-dragging`(C5)、`cardMeta.imageFolder`(C7b)、`focusFieldInSettings`(C8)、`var inView = !(max<dataMin || min>dataMax)`(C9)、`基于 Excel/WPS 超级表`(C10)、`MultiTableAddin.Resources.RibbonOpen.png`(C11)。
- **关键修正点**：① `verify-build.py` 原 `#14 kanban add group button` 检查期望 `function addGroupValue(v)` 存在，但 C6 已按需求**移除**该函数 → 改为**负向检查**（确认 `function addGroupValue(v)` / `＋分组` / `addGroupValue` 均不存在）；② Phase C 初版字符串误用 `openBatchFieldEditor`/`datetimeInputControl`/`.kanban-card.dragging{opacity:.65}`，Grep 核对实际代码后修正为上述精确子串。

### C11 图标栅格化（Pillow 直接绘制 DWB SVG）
- 源：`temp/DWB_TB-01-01.svg`（viewBox 0 0 70.871 70.859）。几何：星形双子路径（fill 黑）、open 圆角矩形表框（stroke #000 width 2.8346）、2 竖线 x=18.186/28.436（y 7.083→57.281）、2 横线 y=15.554/26.768（x 6.634→56.831）。
- **运行时**：受管 Python venv 不存在（`C:/Users/LQ/.workbuddy/binaries/python/envs/default` 缺失），改系统 Python `C:/Python314/python.exe`（Pillow 12.2.0 可用）。inkscape/resvg/rsvg-convert/cairosvg 均缺失 → 重写 `temp/render_dwb_icon.py` 用 **Pillow 直接解析 SVG 几何绘制**（parse_path 处理 M/m/L/l/C/c/Z/z + 三次贝塞尔采样；SCALE=256/70.871；表框用 `draw.line(joint='curve')`；4 条线 width=round(2.8346*SCALE)）。
- 输出：`Resources/RibbonOpen.png`(32×32) + `Resources/Icons/RibbonOpen.png`(32×32) + `Resources/Icons/AppIcon.ico`（多尺寸 16/32/48/64/128/256）。C# 实际加载前者（`RibbonController.cs` 白名单 `RibbonOpen`）。
- 注：Read 该 PNG 被模型过滤（"不支持图像"），跳过肉眼校验，改由 `verify-build.py` 资源名子串确认已嵌入。

### C3 拖拽说明文档（用户明确要求"单独写一份 MD"）
- 新建 `MultiTableAddin/docs/drag-interaction.md`。内容：① 公共基础设施（fmtDate/fmtDateTimeLocal/updateCell 保存通道）；② 日历日/周拖拽（PX_PER_MIN、snap、整体移动/顶把手调开始/底把手调结束三模式坐标换算）；③ 日历月拖拽（delta 单元格换算天数、时间分量继承）；④ 甘特拖拽（ganttRange 缩放、百分比定位、RESIZE_EDGE=8、alignDate 对齐、日期反算公式）；⑤ 关键参数速查表；⑥ 交互时序；⑦ 注意事项（本地时间字符串、日/周纵向月横向、吸附仅影响保存值）。

### 关键易错点（可复用）
- **配置往返丢属性**：JS 视图自定义属性若不在 C# `ViewConfig` 模型里，`JsonSerializer.Deserialize<ViewConfigFile>` 反序列化时被丢弃 → 任何需持久化的新字段都要同步加 C# 模型（C4 colorField、C7b imageFolder 等）。
- **构建两步法不可省**：只 `dotnet build` 不出到 dist，重装仍加载旧资源；覆盖 dist dll 先 rm 再 cp。
- **verify-build 负向检查**：已被移除的功能（如 C6 的 addGroupValue）要用"确认不存在"断言，不能再用"存在"断言。
- **图标栅格化无 SVG 渲染库时**：Pillow 几何重绘（解析 path + 贝塞尔采样），SCALE 以 viewBox 短边为基准，LANCZOS 降采样。

### 待用户现场复核（本环境无 Excel/WebView2）
- C3 甘特三视图拖拽手感与实时写回；C4 自动分色；C5 看板拖拽无误选且分组实时更新；C7b 图片按字段匹配；C8 字段设置随焦点列；C11 功能区图标为 DWB。
- 重装前完全关闭已加载该 XLL 的 Excel/WPS；重装后「关于」版本应为 Phase C 对应版本。

## 2026-08-12 Phase D 需求落地（#273–#283，全完成，版本升至 2.9.7）

### 范围（10 项显式需求，原编号 1–4、6–10，缺 5）
- #273 需求9：功能区图标换成 `icon-table.html` 指定的 `tabler-icons:outline:table-spark`（星形+表框+横竖线）。
- #274 需求4：图表标签隐藏阈值 `minLabelValue` 参数，合并"隐藏0值"为单一控制（值 ≤ 阈值则不显示标签）。
- #275 需求6：窗口默认最大化打开（`FormWindowState.Maximized`）。
- #276 需求7：表格汇总行位置参数 `summaryPosition`（DataEnd 数据末尾 / WindowEnd 窗口末尾，CSS `position:sticky;bottom:0`）。
- #277 需求3：甘特加"季"视图切换按钮（ganttRange 加 `quarter:90`、`ganttUnitStart`/`ganttStep`/`ganttTickLabel`/`ganttUpperUnit` 补 quarter 分支）；核查"今天"按钮无用 → 按用户"无用就删"指示已从 `renderGantt` 控制行移除。
- #278 需求2：日历选中详情数据不一致修复（`renderTimeGrid` 事件源由 `state.rows.filter` 改为 `getVisibleRows().filter` 以尊重筛选）+ 日/周视图选中任务加深蓝边框（`.cal-day-event.selected{outline:2px solid #003366}`）；`selectRow` 跨视图统一清除/重选 `.selected`。
- #279 需求10：看板拖拽到新分组后实时更新（`up()` 回写 `values[groupBy]=newVal` 并 `delete rr.displayTexts[groupBy]` 清缓存，`.then` 内 `renderContent()` 重渲）。
- #280 需求1：日历月视图任务横向拖动调整起止日期（时间分量不变，`makeMonthEventDraggable` 全量重写：move / resize-start / resize-end 三模式 + ghost + `_dragMoved` 防误触详情面板）。
- #281 需求8a：批量编辑窗口加宽（`maxWidth` 920→1200px）+ 表格加 `colgroup`/各输入框 `width:100%;box-sizing:border-box` 以匹配主窗口方格样式。
- #282 需求8b：基于 `多维插件字段处理方案.txt` 生成「多维表字段处理优化方案.md」文档。
- #283：统一构建 + JS 语法校验 + dist 更新 + verify-build 验证。

### 关键实现点
- **配置往返同步（仍是最易错点）**：新增 `ChartConfig.MinLabelValue`、`SummaryPosition` 枚举 + `ViewConfig.SummaryPosition` 均同步写入 C# 模型，并回写 `saveConfig`（`cv.summaryPosition=sv.summaryPosition`、`item.minLabelValue=...`），否则重载 DLL 时被 `JsonSerializer.Deserialize` 丢弃。
- `ganttRange` 缩放等级改为 `{day:3, week:21, month:70, quarter:90}`。
- 图标栅格化：`temp/render_table_spark.py`（Pillow 几何重绘 table-spark 路径：星形 / 表框 `M12 21h-7a2 2 0 0 1 -2 -2v-14...` / 横线 `M3 10h18` / 竖线 `M10 3v18`）。受管 venv 无 Pillow → 用 `C:/Users/LQ/.workbuddy/binaries/python/versions/3.13.12/python.exe -m pip install --user Pillow`（Pillow-12.3.0）重绘，输出 `Resources/RibbonOpen.png`、`Resources/Icons/RibbonOpen.png`、`Resources/Icons/AppIcon.ico` 覆盖。

### 构建与验证（铁律两步法）
- `cd MultiTableAddin/src/MultiTableAddin && dotnet build -c Release`：**0 错误**，产物 `bin/Release/net8.0-windows/MultiTableAddin.dll`。
- 版本号 `Models.cs`：`Version="2.9.7"`、`ReleaseDate="2026-08-13"`（原 2.9.6 / 2026-08-12）。
- dist 更新：`rm -f MultiTableAddin/dist/files/MultiTableAddin.dll && cp .../bin/Release/.../MultiTableAddin.dll MultiTableAddin/dist/files/MultiTableAddin.dll`（dll 714240 字节）。先 rm 再 cp 规避句柄锁 Permission denied。
- JS 语法：`check-js-syntax.js`（`new Function` 校验内联 script）→ **JS syntax OK**（注意 `node --check index.html` 会因 HTML 扩展名报 ERR_UNKNOWN_FILE_EXTENSION，故走此脚本）。
- `verify-build.py`：最后修正 D2/D3/D4/D6 编码（DLL 中 `MinLabelValue`/`SummaryPosition`/`FormWindowState` 实为 UTF-8 而非 UTF-16LE）→ 重跑 **全绿（exit 0，0 FAIL）**：含 D1 季按钮、D2 `MinLabelValue`、D3 `FormWindowState`、D4 `SummaryPosition`、D5 深蓝边框 `#003366`、D6 `delete r.displayTexts[u.field]`、D7 批量窗 1200px，以及全部旧检查（#1 标签阈值、`#7` sticky-summary、C3 缩放含 quarter 等）。

### 新增/改动文件清单
- `MultiTableAddin/src/MultiTableAddin/Core/Models.cs`（版本 + MinLabelValue + SummaryPosition）
- `MultiTableAddin/src/MultiTableAddin/Html/HtmlMainForm.cs`（默认最大化）
- `MultiTableAddin/src/MultiTableAddin/Html/index.html`（需求1/2/3/4/6/7/10/8a 全部 JS/CSS 改动）
- `MultiTableAddin/Resources/RibbonOpen.png`、`MultiTableAddin/Resources/Icons/RibbonOpen.png`、`MultiTableAddin/Resources/Icons/AppIcon.ico`（table-spark 图标）
- `temp/render_table_spark.py`、`temp/icon-table.html`（图标源）
- `多维表字段处理优化方案.md`（#282 文档）
- `verify-build.py`（版本串 + D1–D7 检查）

### 待用户现场复核（本环境无 Excel/WebView2）
- #273 功能区图标为 table-spark；#277 季视图切换 + "今天"按钮已删；#278 日历详情一致性 + 深蓝选中边框；#279 看板实时更新；#280 月视图横向拖动改期；#275 默认最大化；#276 汇总行位置切换；#281 批量窗加宽样式。
- 重装前完全关闭已加载该 XLL 的 Excel/WPS；重装后「关于」版本应为 **2.9.7**。
