## 2026-08-11 多维表分析插件改动

### 需求修复与优化
1. **表单视图输入宽度**（#1）：`.form-row .fval` 改为 `flex:0 0 33.33%`，标签最大宽度缩至 160px。
2. **新增记录自动定位编辑**（#2 前半）：`addRow` 新增后通过 `openTable(..., afterRender)` 回调刷新数据，并自动 `showDetailPanel(newIdx)` 打开右侧详情抽屉。
3. **字段设置改为右侧抽屉**（#2 后半）：新增 `#settingsPanel` DOM 节点；`showFieldSettings` 不再使用模态弹窗，而是渲染为右侧抽屉，与记录详情互斥；保存字段后实时 `renderContent()` 更新主视图。
4. **必填复选框尺寸与对齐**（#2）：CSS 控制 `#settingsPanel/.detail-panel input[type=checkbox]{width:16px;height:16px;margin:0}`，与左侧标签左对齐。
5. **看板/详情面板字段标签正常宽度**（#3）：`.detail-panel .form-field label` 改为 `width:auto;min-width:60px;max-width:120px`。
6. **字段显隐面板修复**（#4）：移除标题；`toggleFieldPanel(btn)` 根据按钮 rect 左下对齐定位；添加 `mouseleave` 自动关闭与点击外部关闭；显隐变更后实时 `renderContent()`。
7. **日历日/周/月布局重构**（#5）：
   - 日/周视图新增左侧 00:00-23:00 时间纵轴。
   - 周视图横向 7 天，每天一列，顶部显示星期+日期。
   - 月视图保持月份网格。
   - 单击日期不再切换视图，而是调用 `showDateDetailPanel(date)` 在右侧抽屉显示该日所有记录列表，点击记录卡片进入单条详情。
8. **表格列宽按内容自适应且不随窗口**（#6）：`table.grid` 移除 `min-width:100%` 改为 `width:auto`，由 `autoFitColumns` 按最长字符计算每列宽度，表格总宽由列宽决定。
9. **看板分组颜色区分**（#7）：`renderKanban` 为每个分组列按索引从 8 色数组取色，设置列头 `border-top` 与列背景淡色。

### 版本与构建
- `AppVersion`：2.2.0 → 2.3.0，ReleaseDate → 2026-08-11，CodeName → CalendarUX。
- `build_addin.ps1 -Configuration Release` 成功（0 错误，13 个既有警告）。
- `dist/files/MultiTableAddin.dll` 已更新（569856 字节，2026-08-11 10:16），已验证含 `2.3.0`/`CalendarUX`/`settingsPanel`/`showDateDetailPanel`/`cal-timeline` 等新代码。

### 构建注意
- 本环境 PowerShell 默认沙箱会拦截 `dotnet`/文件写入，构建与验证需使用 `dangerouslyDisableSandbox=true`。
- 重装插件前请完全关闭已加载该 XLL 的 Excel/WPS。

### v2.4.0 / v2.4.1 视图 UX 重构 + 甘特修复（2026-08-10/11）
- 11 项视图 UX 需求（日历月按钮、字段开关实时生效、表单按钮右对齐、新增后定位+右侧字段设置抽屉、分组「不分组」、显隐改名「字段开关」、左侧栏保留+顶栏移除+按钮左移、表格行右键菜单插入/删除/详情、日期与下拉单元格用列全宽、编写视图切换结构 MD 文档）全部落地，版本 2.4.0（CodeName RightPanelUX），并产出 `视图切换结构设计.md`。
- 关键修复：字段显隐改为「每视图独立 visibleFields」——openTable/renderSidebar 从当前视图自己的 visibleFields 初始化，saveConfig/toggleFieldPanel 写回 activeViewId 对应视图（彻底解决显隐无效）。
- 表格行右键菜单：`showRowContextMenu` + C# `InsertRow`（Excel ListObject `listRows.Add(position)`）。
- **v2.4.1（CodeName GanttAlways）真正修复 #11 甘特缺失**：根因是侧栏只渲染 config 中已有的视图，老配置/旧版本未生成甘特视图入口；在 `ensureViewDefaults()` 中，当数据存在日期字段且无甘特视图时自动补建并持久化，侧栏即出现「甘特视图」。renderGantt 本身对空 ganttConfig 有占位提示。已构建验证（DLL 578560 字节，含 2.4.1/GanttAlways/hasGantt/gantt- 等标记，0 错误）。

### ⚠️ 深刻教训：改完源码必须构建部署（2026-08-11 下午）
- 用户反馈「前面的问题并没有解决」的根因：**v2.5.0 的源码改动全部完成，但漏跑了 `build_addin.ps1`，dist/files 里仍是旧的 v2.4.1 DLL（11:27:45）**，Excel 加载的仍是旧资源，所以所有修复都"看不到"。
- 必须在源码改动后执行 `build_addin.ps1 -Configuration Release`（PowerShell + `dangerouslyDisableSandbox=true`）才会把产物写入 `dist/files`；仅 `dotnet build` 不会更新 dist。
- 交付前务必用 `[System.IO.File]::ReadAllBytes` + UTF8 字符串 `IndexOf` 校验部署 DLL：版本号 + 关键改动标记（如 `年Q`/`着色字段`/`group-header`/`appendTableRow`/`GANTT_PALETTE`）全部 FOUND 才算真正部署成功。

### v2.5.0（CodeName ViewPolish）10 项需求 + 补全（2026-08-11 13:49 部署）
- 源码版本 2.5.0 / ViewPolish / 2026-08-11；DLL 584704 字节，0 错误。
- #1 表格右侧详情面板常驻并随焦点实时刷新（renderContent 后若详情已开则 showDetailPanel(selectedRow)；selectRow 内同步）。
- #2 日期/组合框列 autoFitColumns 额外 +22px 防编辑态宽度抖动；编辑控件 td.editing 占满列宽。
- #3 单元格编辑后真正提交（editCell blur/Enter 调 updateCell 并更新 r.values，不再回退）。
- #4 **表格分组真正补全**：renderTable 此前只平铺，现按 activeView.groupBy 渲染分组头行（带计数，Select/Quarter 按选项顺序），抽出 appendTableRow 复用；工具栏「不分组」清空分组。
- #5 看板拖拽 drop 已 updateCell+renderContent，逻辑正确（部署后应实时生效）。
- #6/#7 字段开关：各视图右侧详情面板均按本视图 visibleFields 显示；标题统一「详情」；长文本 textarea 自动高度。画册/看板设置子窗已无冗余「卡片标贴」（#6 部分——"卡片标题移到主窗口按钮、移除子窗口"属后续重构）。
- #8 甘特：移除「日」按钮，顺序改 周/月/季/年；新增「着色字段（按状态分色）」设置，renderRow 按字段值取 GANTT_PALETTE 颜色填充进度条。
- #9 日历月按钮在源码存在（calMonth），随部署生效。
- #10 甘特季度标签 `2026Q2` → `2026年Q2`（飞书式）。

### v2.6.0（CodeName KanbanDashboardUX）日历/看板/全局/仪表盘优化（2026-08-11 14:52 部署）
- 源码版本 2.6.0 / KanbanDashboardUX / 2026-08-11；DLL 596480 字节，0 错误（构建时 dist/files 部分文件被占用，.xll 等需用户关闭 Excel 后重装，但 MultiTableAddin.dll 已更新）。
- 日历：月按钮区加 `flex-wrap` 防止被挤出；新增记录后打开右侧详情抽屉供编辑保存，不再直接 toast。
- 看板：字段开关生效（卡片显示除标题外的可见字段）；分组字段仍由工具栏「不分组」下拉控制；「卡片描述」改为「卡片标题」并移到工具栏下拉；移除卡片设置子窗口冗余项；分组列头色条改用 `box-shadow:inset` 避免越界。
- 排序下拉默认项由「排序」改为「不排序」；日期选择器当前日期高亮改为主蓝色，滚轮默认一日一跳（shift 三月/ctrl 一年）。
- 全局：移除各视图「⚙ 视图设置」按钮；日历改用「日历设置」弹窗保留日期/标题字段两个功能；表单视图标签固定 100px、输入区 `flex:1` 等长左对齐；日期输入空值默认今天；Percentage 输入按百分比数值编辑保存；textarea 自动高度额外加一行行高。
- 仪表盘：每个指标/图表卡片加「设置」按钮，添加时自动打开设置弹窗；设置项全部中文（图表类型、聚合方式、时间维度）；工具栏保留「＋ 图表」「＋ 指标」并新增「保存设置」按钮。
- 图表视图设置中的类型/聚合/时间维度也改为中文选项。

### v2.7.0（CodeName CalendarDetailUX）7 项需求修复（2026-08-11 15:36 部署）
- 源码版本 2.7.0 / CalendarDetailUX / 2026-08-11；DLL 603136 字节，**0 错误**，构建产品已写入 `dist/files`（EXITCODE=0）。
- 部署校验（UTF8+UTF16 双解码 DLL）：`2.7.0`/`CalendarDetailUX`/`2026-08-11` FOUND；功能标记 `cal-current-line`/`renderTimeGrid`/`ganttRange`/`endDateField`/`dragging`/`dropover`/`allday`/`__mtBuild` 全部 FOUND。
- #1 右侧详情 + 模态编辑**显示所有字段**（不受字段开关控制）：`showDetailPanel`(948) 与 `openRecordModal`(911) 由遍历 `state.visibleFields` 改为遍历 `state.fields`。
- #2 看板拖拽**实时更新 + 高亮**：drop 先更新本地 `state.rows` 再 `renderContent()` 立即重绘，然后异步 `updateCell` 持久化；`dragstart` 加 `.dragging` 高亮（旋转/阴影/强调色背景），目标列 `.dropover` 虚线高亮；`dragend` 清理 `_dragRow`。
- #3 看板分组色条**不越界**：`.kanban-col` 加 `overflow:hidden`，`.dropover` 用 `outline-offset:-2px` 内描边替代外扩。
- #4 全局输入控件**统一对齐**：`.form-field label` 固定 `width:120px;text-align:right` + 省略号；输入框 `width:100%`；`.setting-row` 同步（label 110px，控件 flex:1）；`#settingsPanel .form-field label` 改 90px。
- #5 日历**月切换按钮**：按钮区重排为 月/周/日 顺序，加 `title` 提示与 `white-space:nowrap`，解决"没有月切换按钮"误判。
- #6 甘特**维度切换真正重绘**：新增 `ganttRange(dim,anchor,dataMin,dataMax)` 由 `anchorDate`+维度跨度计算可见时间窗，切换周/月/季/年时图表（进度条）随之变化；新增「今天」按钮重定 anchor。
- #7 日历周/日视图**自由添加记录 + 红线 + 拖拽调时**：周/日重构为统一 `renderTimeGrid`——时间纵轴 + 当天红色 `cal-current-line` 当前时刻线；点击空白格 `addRow`（dateField 默认该格时间，endDateField +1h）；事件可拖动改时间、底部 `cal-event-resize` 拖拽改时长（`updateCalEventTime` 先本地后持久化）；新增可选 `endDateField` 配置（开始/结束日期字段/标题字段）。

### v2.8.0（CodeName InteractionPolish）12 项交互/全局优化（2026-08-11 18:20 部署）
- 源码版本 2.8.0 / InteractionPolish / 2026-08-11；DLL 616960 字节，**0 错误**，构建产品已写入 `dist/files`（EXITCODE=0）。
- 部署校验（latin-1 检索 JS 标记 + UTF-16LE 检索版本串）：`enableKanbanDrag`/`showDraftDetailPanel`/`showGanttSettings`/`gantt-today-line`/`syncLeftSelectionHighlight`/`cal-current-line`/`renderTimeGrid` 与 `2.8.0`/`InteractionPolish`/`2026-08-11` 全部 FOUND。
- 一、#1 **看板拖拽彻底修复**：根因是 WebView2 中 HTML5 DnD（dragstart/drop）不可靠、反复失败；改用自定义鼠标拖拽 `enableKanbanDrag`（`mousedown` 生成 ghost 克隆 + `mousemove` 跟随 + `mouseup` 时用 `elementFromPoint` 定位目标 `.kanban-col`，更新本地 `state.rows` 后 `renderContent()` 并异步 `updateCell` 持久化）。这是用户记忆中"之前正确版本"的实现方式。
- 一、#2 **看板选中卡片高亮**：`.kanban-card.selected` 用强调色边框 + 淡蓝底 + 2px box-shadow；`selectRow` 驱动 `syncLeftSelectionHighlight()` 在各类视图同步高亮。
- 一、#3 **画册卡片最小尺寸**：`.gallery-card{min-width:200px;min-height:230px}`，网格 `minmax(200px,1fr)`+`gap:14px`，数据多时不再被压成一条线。
- 一、#4 **日历完善**：恢复「日」切换按钮并重排为 月/周/日（左）+ 今天（右）；单击空白格改为 `showDraftDetailPanel`（右侧新建草稿，仅点保存才 `addRow`），不再自动建记录；红色当前时刻线移到 `timeline` 容器，`left:46px;right:0` **贯穿所有列**。
- 一、#5 **表单按钮对齐**：`.form-actions{width:100%;justify-content:flex-end;padding:0}`，`.form-row .fval{padding:3px 0}`，按钮行容器与上方标签+输入容器对齐，三个按钮整体右对齐到输入控件右侧。
- 二、#6 **右侧字段标签左对齐并去空白**：`.form-field label`/`.setting-row label` 改为 `flex:0 0 auto;min-width:70px;max-width:130~140px;text-align:left`，不再固定 120px 留白。
- 二、#7 **激活控件蓝色填充高亮**：`:focus` 状态 `border-color:var(--accent);background:var(--accent-weak2);box-shadow:0 0 0 3px var(--accent-weak)`，替换原黑色边框无填充。
- 二、#8 **左侧对应控件高亮**：`syncLeftSelectionHighlight()` 在 `renderContent()` 与 `showDetailPanel()` 末尾，把 `state.selectedRow` 镜像到 `tbody tr`/`.kanban-card`/`.gallery-card`/`.cal-day-event`/`.gantt-row` 的 `.selected` 类，打开右侧面板即知改的是哪条。
- 二、#9 **甘特配置悬停窗口 + 今天线**：视图栏新增「甘特图配置」按钮 → `showGanttSettings()` 浮层（开始/结束日期字段、标题字段、颜色模式 自定义/按字段 + 调色板 10 色 + 自定义取色 + 仅工作日）；`ensureViewDefaults` 甘特默认 `colorMode/colorField/customColor/workdaysOnly`；新增 `.gantt-today-line`（红 2px，`calc(168px + (100%-168px)*tPos/100)`）贯穿图表区。
- 二、#10 **环形图修复 + 删除图表**：`drawChart` 环形图补白色 `<circle>` 实心孔（r*0.55）；仪表盘图表卡与独立图表视图均加删除按钮（分别移除 `dashboardConfig.charts` / 从 `state.views` splice）。
- 二、#11 **删除统计图表视图**：`ensureViewDefaults` 移除 `Chart` 分支，`renderSidebar` 对 `viewType==='Chart'` 直接 `return`，`openTable` 的视图检索排除 Chart。
- 二、#12 **左侧视图文字加大**：`.sidebar{width:118px}`、`.group-title{font-size:12px}`、`.view-item{height:28px;font-size:13px}`，宽度不变仅字号加大。

### v2.9.0（CodeName GanttDashboardPolish）11 项需求修复 + 测试数据 + 帮助文档（2026-08-11 20:14 部署）
- 源码版本 2.9.0 / GanttDashboardPolish / 2026-08-11；DLL 622592 字节，**0 错误**，EXITCODE=0。构建时 `.xll` 及部分依赖 DLL 因 Excel/WPS 进程占用被跳过，但 `MultiTableAddin.dll` 已正常写入 `dist/files`；`.xll` 为 stub，加载的是新版 DLL，功能已更新。
- 部署校验：`2.9.0`/`GanttDashboardPolish`/`2026-08-11` FOUND；`showLabels`/`datetime-local`/`cal-more`/`visibleFields`/`gantt-label` 等新增标记 FOUND。
- #1 **日历当前时间线位于今天列**：`.cal-current-line` 由 `timeline` 全局线改为放入今天 `.cal-day-col-body` 内，`left:0;right:0`，红色圆点落在当前日期列。
- #2 **甘特字段开关生效**：`renderGantt` 左侧标签优先使用 `state.visibleFields`（排除起止/进度字段），未开启字段时回退到 `cfg.labelField`；字段开关增删字段会直接影响甘特标签显示。
- #3 **甘特百分比分组头格式化**：分组 key 改用原始值 + `formatValue(gf, raw)`，百分比字段分组头由 `0.5` 显示为 `50.00%`。
- #4 **仪表盘条形图修复 + 标签开关**：修复 `drawChart` 横向条形高度误用 `plotH*value/maxV` 导致重叠的问题；条形图改用独立左侧标签区（padL=70），分类标签居左、数值标签在条尾。`groupAggregate` 在维度为空但选了时间维度时自动使用首个日期字段。新增图表 `showLabels` 配置与仪表盘工具栏「显示标签 / 隐藏标签」全局开关。
- #5 **测试数据**：生成 `多维表测试数据_插件测试.xlsx`，43 条记录，6 人×6 条（串行+并行），覆盖 1-12 月；字段含任务ID/名称/内容/姓名/部门/日期时间/截止日期/完成进度/当前状态/费用/盈利/耗时/总结/优先级/标签/季度；另加 7 条刘伟 8 月 10 日密集记录用于测试日历折叠提示。
- #6 **日历拖拽时间实时预览**：`makeCalEventDraggable` 拖动时实时更新事件条上的 `.evt-time` 文本，显示目标开始/结束时间。
- #7 **日历月视图最多 5 条 + 还有 N 条提示**：`renderMonthView` 由 `slice(0,3)` 改为 `slice(0,5)`，超出时添加 `.cal-more` 浅灰提示，点击可查看当日全部记录。
- #8 **日历各视图点击空白添加记录**：月/周/日视图点击空白格/日期格均调用 `showDraftDetailPanel` 预填日期时间；Date 字段预填日期，DateTime 字段预填 `YYYY-MM-DDTHH:MM`。
- #10 **日历日切换按钮防被挤出**：`.calendar-head .btn{flex-shrink:0;white-space:nowrap}`，标题仍可收缩，确保 月/周/日 按钮始终可见。
- #11 **仪表盘标签全局开关**：仪表盘视图栏新增「隐藏标签 / 显示标签」按钮，批量切换所有图表 `showLabels`。
- #12 **表格日期/下拉列宽稳定**：`autoFitColumns` 中 Date/DateTime 最小宽度提至 170px，Select/Quarter 提至 150px，额外加宽由 22px 提至 34px；`select.cell-edit` 增加 `padding-right:20px`，避免编辑态文本被箭头遮挡。

### v2.9.1（CodeName IconStickyPolish）8 项需求修复（2026-08-11 21:17 部署）
- 源码版本 2.9.1 / IconStickyPolish / 2026-08-11；DLL 644608 字节（dist/files 时间戳 21:17，与构建一致），**0 错误**，EXITCODE=0。
- 部署校验（UTF-16LE 检索版本串 + latin-1 检索 JS 标记）：`2.9.1`/`IconStickyPolish`/`2026-08-11` 全部 FOUND；`cal-view-btns`/`showLabels`/`dt-split`/`gantt-sticky`/`timeline`/`RibbonOpen` 全部 FOUND。
- #1 **日历日视图切换按钮**：`renderCalendar()` 头部把 月/周/日 按钮包进 `<span class="cal-view-btns">`（flex + `flex-shrink:0;white-space:nowrap`），标题区 `.calendar-wrap`/`.calendar-head` 加 `min-width:0` 允许收缩，确保三按钮始终可见。
- #2 **仪表盘标签开关仅控制值标签**：`drawChart` 中分类/坐标轴标签（category/axis `lbl`）移到 `if(cfg.showLabels!==false)` 之外**始终绘制**，仅数值标签 `valLbl` 受 showLabels 控制；工具栏「显示/隐藏标签」仍批量翻转所有图表的 showLabels。柱/条分支统一处理。
- #3 **表单三按钮右对齐**：`renderForm()` 把操作行由「card 外独立 append」改为 `card.appendChild(actRow)` 并加类 `form-row form-actions-row`，按钮整体右对齐到输入控件右侧（无多余底部边框）；重置按钮额外重初始化 textarea 高度。
- #4 **日历双向拖拽 + 起止时间预览**：`makeCalEventDraggable` 拖动时生成 fixed 定位 ghost 克隆（跟随光标，原事件 `opacity:0.35`），第 1 行标题、第 2 行 `formatRange(totalMin)` 显示「开始 - 结束」实时时间（如 06:45 - 09:30）；整体移动与底部 resize 均走 ghost；支持上/下拖拽并 clamp 0–920px。
- #5 **甘特顶部标签/控件置顶**：`renderGantt()` 把头部 + 时间轴包进 `.gantt-sticky`（`position:sticky;top:0;z-index:10;background:var(--bg)`），滚动大量项目时日期标签与周/月/季/年等维度控件始终可见可用。
- #6 **甘特当前日期红线减半**：`.gantt-today-line` 宽度由 2px 改为 1px。
- #7 **右侧面板日期时间分离输入**：新增 `buildDateTimeSplitInput(f, raw)`（date `<input type=date>` + `<select>` 时间 00:00–23:30 步进 30 + 23:59，默认 09:00），`readDateTimeSplitInput` 拼回 `d'T't+':00'`；`buildPanelInput` 路由 DateTime→分离输入，其余走 `buildFieldInput`；已接入 `renderForm`/`openRecordModal`/`showDetailPanel`/`showDraftDetailPanel`/`resetBtn`，选择器扩展 `.ctrl .dt-split`。参考飞书右栏分离式日期时间录入。
- #8 **icon-design 技能匹配图标 + 精简功能区**：用 icon-design 技能（tabler-icons 库）匹配 11 个 SVG 图标（table/refresh/pin/info/form/kanban/gallery/calendar/timeline/dashboard/chart），注入 HTML 内联 `ICONS` 映射，侧栏视图项与顶栏按钮改用内联 SVG（14px）。导出 `RibbonOpen.png`/`RibbonAbout.png`/`AppIcon.ico`（tabler #3370FF）嵌入资源；`Ribbon.xml` 精简为仅「打开多维表」(`image="RibbonOpen"`) 与「关于」(`image="RibbonAbout"`)，移除刷新数据/保存配置/运行时/工具组等无用按钮；`RibbonController.cs` 移除 3 个处理函数、新增 2 个图像 ID；`HtmlMainForm.cs` 加载嵌入 `AppIcon.ico` 作为窗体图标。
