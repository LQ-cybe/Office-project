# 2026-08-16 工作日志

- 用户提出四项功能扩展需求分析（画册圆形图片、甘特拖拽+保存模式、视图另存、HTML图表视图）。加载 `exceldna-addin-builder` 技能，并核查当前 `index.html`(2.9.17) 真实代码后，撰写可行性分析 MD：`扩展功能可行性分析.md`。
- 关键代码定位：甘特 `makeGanttBarDraggable`(2718) 已有 move/resize 且按 range 换算（跟随缩放），但拖起即 `updateCell` 即时写回(2771)——故"手动保存模式"核心是引入脏缓冲+关闭异步拦截；画册 `cardMeta` 已持久化（加 `imageShape` 字段即可）；`state.views`/`cfg.views` 视图数组已存在（另存=克隆快照）；`dash.html` 根目录已存在（作 HTML 视图样板）；`APP_SETTINGS`(531) 为软件级设置通道。
- 建议优先级：①画册圆形(低)→③视图另存(中)→②甘特保存模式(中高)→④HTML视图(中)；②④独立，②③共用 `cloneCurrentView()`/`saveConfig` 通道。

## 下午（文档精修 + 目录整理）
- 按用户三点反馈精修 `扩展功能可行性分析.md`：
  1. **功能三视图删除规则纠正**：基础视图（表格/表单/看板/画册/日历/甘特/仪表盘/图表 8 类）不可删除，仅"另存为"产生的用户视图可删；重命名/删除入口优先放进右键菜单（`contextmenu`，难度低）。
  2. **功能四引用 `大屏结构说明.md` 五层分离**：HTML 图表视图应采用 引擎壳/渲染库/数据/主题(配色)/全局设置/布局(模板) 模块化结构，`dash.html` 重构为首个内置模板；配色/模板/框架外置便于独立切换。参考文档位于 `D:/编程开发/开源项目/Office/大屏项目管理/大屏示例/`。
  3. **新增 §8 Ctrl+滚轮缩放控制可行性**：根因=`HtmlMainForm.cs` 初始化未设 `IsZoomControlEnabled`（默认 true→WebView2 原生整页缩放）；方案1全局关闭（1行，推荐）、方案2禁用内置后在 `index.html` 按 `activeViewType()`(805) 仅特定视图自定义缩放。两者均插件内闭环。
- 目录整理：新建 `参考资料/` 文件夹，移入 8 份明确外部参考文档（飞书WPS对比、JS甘特组件、任务条拉伸失败参考资料、优化问题点、无标题-1、Skill分析、甘特图独立平移、日历任务条拖拽拉伸相关内容）；根目录保留项目自身文档（4份核心规划+开发记录/对照表/结构设计/帮助等）与代码/测试数据不动。

## 上午（实现 v2.9.18 两项功能 + 清理 + 记录 + 帮助）

按用户要求（基于 `扩展功能可行性分析.md`）落地 ① 画册圆形图片显示模式、② 甘特拖拽保存模式开关，并完成临时文件清理、更新记录、文档与软件内帮助更新（后三点"每次改动都清理并记录"；且**绝不修改用户 `.multiview.json` 设置文件**）。版本升到 **2.9.18 / GalleryCircleGanttSave**。

- **① 画册圆形（#400）**：`Models.cs` 的 `CardMeta` 加 `ImageShape` 字段（反序列化不丢字段）；`index.html` 加 `.gimg.circle` CSS（`border-radius:50%`+`object-fit:cover`）、`renderGalleryInner` 按 `cfg.cardMeta.imageShape==='circle'` 加 circle 类、`showViewSettings` 画册分支新增"图片显示模式"下拉并写入 `makeApply`；HELP 画册小节补充圆形说明。仅写 `cardMeta`（视图配置），不碰软件设置文件。
- **② 甘特保存模式（#402）**：`defaultAppSettings` 加 `ganttSaveMode:'auto'`；`loadAppSettings` 从 localStorage 读取（已修复此前漏写）；`toggleSettingsPanel` 加"可视化调整保存模式"行（切回自动且脏→flush，切手动且脏→showGanttSaveBar）。`makeGanttBarDraggable` 的 `onUp` 在 manual 下改为脏缓冲（`markGanttDirty` 暂存内存、toast 提示），auto 下仍拖完即 `updateCell` 写回。新增助手：`ganttPendingStore`/`markGanttDirty`/`showGanttSaveBar`/`hideGanttSaveBar`/`flushGanttEdits`/`discardGanttEdits`/`resetGanttDirty`/`pushVisualEditDirty`/`guardUnsavedVisualEdits`/`showConfirm3`/`window.__mtFlushVisualEdits`。侧栏视图点击与数据源切换经 `guardUnsavedVisualEdits` 三选一确认（保存/放弃/取消）。C# `HtmlMainForm.cs` 加 `_visualEditDirty/_visualEditCount/_forceClosing` 字段、`setVisualEditDirty` 分发、异步 `HandlePendingVisualEditsOnClose`（MessageBox 三选一，取消保留窗口）、`FormClosing` 非强制且脏时 `e.Cancel=true` 拦截。保存模式仅针对时间/日期值拖拽提交，与视图布局 `saveConfig()` 解耦。
- **清理（#401）**：删 build.log/icon-*.log/js-syntax.log/pngdims*.txt/ps-test.txt/temp//bin/obj/dist/files/logs 等临时产物；保留 `MultiTableAddin/dist/files/` 交付物、源码、测试数据、用户文档。`~$多维表测试数据_插件测试.xlsx`（Excel 锁文件）因 Excel 占用无法删，关闭后自动消失（非阻塞）。
- **记录与文档（#403/#405）**：`项目开发记录.md` 头部改 v2.9.18、版本表加行、新增「七、v2.9.18 更新内容」整节；`扩展功能可行性分析.md` §2/#400、§3/#402 末加「实现状态 ✅ 已实现」并校准与原文差异；`软件设置与文件设置对照表.md` 软件设置表加"可视化调整保存模式"行；`index.html` HELP 画册/甘特小节补充圆形与保存模式说明。
- **构建验证（#404）**：`node check-js-syntax.js`→JS syntax OK；`dotnet build -c Release`（C:/Program Files/dotnet/dotnet.exe）→0 错误；手动 `rm -f MultiTableAddin/dist/files/MultiTableAddin.dll && cp` 更新交付（最终 812544 字节，时间戳 Aug 16 09:40）；`verify-build.py`（managed python3 `C:/Users/LQ/.workbuddy/binaries/python/versions/3.13.12/python3`）全绿（#400×2 + #402×13 全 OK，无 FAIL）；末次 `rm -rf .../bin obj` 清理构建产物，确认 dist 三交付物完好。
- **约束校验**：未改任何 `.multiview.json` 用户设置文件；甘特模式走 localStorage 默认 auto（兼容旧行为），画册圆形走 cardMeta（视图配置）。

## 傍晚（v2.9.19 甘特交互优化 #406–#409）

按用户 4 点反馈，针对甘特视图做交互打磨，版本升到 **2.9.19 / GanttInteractTune**（Models.cs）。

- **#406 保存确认弹窗改软件风格**：关闭拦截链路 C# `HandlePendingVisualEditsOnClose` 不再用 `MessageBox.Show`（VBA 风格），改为 `ExecuteScriptAsync("window.__mtPromptVisualEditClose()")`；JS 新增 `__mtPromptVisualEditClose`（调 `showConfirm3` 三按钮「保存/不保存/取消」）、`__mtDiscardVisualEdits`（调 `discardGanttEdits`）、`__mtFlushVisualEdits`（返回 `flushGanttEdits()` Promise，便于 C# await 写回完成再关窗）。C# 新增 `ResolveVisualEditClose(pars)`：按 `choice` 执行 save/discard/cancel；cancel 保持窗口，save/discard 后 `_forceClosing=true; Close()`。Dispatch 在 `setVisualEditDirty` 后加 `case "resolveVisualEditClose"`。`guardUnsavedVisualEdits` 文案精简为「有未保存的甘特图调整，是否保存？」，按钮「保存/不保存/取消」。
- **#407 两端 resize 手柄**：`makeGanttBarDraggable` 新增 `RESIZE_EDGE=10`，按 `x` 判定 `resize-l`/`resize-r`/`move` 三模式；resize-r 固定左端调宽、resize-l 固定右端调左、move 整体平移，均复用原百分比换算。`renderRow` 在 bar 内追加 `⟨`/`⟩` 两端手柄（`div.gantt-resize.gantt-resize-l/r`，CSS `ew-resize`，hover 加深）。`.gantt-bar` 加 `cursor:grab`/`:active{cursor:grabbing}`。
- **#408 移动后视图稳定（水平滚动条不跳变）**：自动模式拖完 **不再整体 `renderGantt()`**（原罪是重渲染导致水平滚动条跳回），仅刷新 `bar.title` 并调 `ganttCenterBarIfNeeded(bar)`；手动模式同样只在需要时居中。新增 `ganttCenterBarIfNeeded(bar)`：用 `getBoundingClientRect` 测 bar 与滚动容器矩形，若 `within`（bar 在视口内 `br.right>sr.left+2 && br.left<sr.right-2`）则 `return` 不调视图；否则按 `leftPct+widthPct/2` 算中心日期 `ganttCenterOnDate`（纯平移 scrollLeft）。实现用户诉求：视口内移动不调视图、移出才水平居中该条。新增 `ganttBarTooltip(cfg,r)`。
- **#409 行高控制**：引入 CSS 变量 `--gantt-row-h`，同时作用于 `.gantt-row`/`.gantt-cell`/`.gantt-fz-row`/`.gantt-track-wrap`/`.gantt-bar`/`.gantt-progress`/`.gantt-bar-label`（track/bar/progress 用 `calc(var - 12px)`，行用 `var`），`renderGantt` 按 `cfg.rowHeight`（`compact/normal/loose`→30/44/58px）经 `wrap.style.setProperty` 注入，分组行 `gh/fz` 同步。甘特字段设置分支（`showViewSettings`）在字段设置后加「行高」下拉（紧凑/正常/宽松），经 `makeApply` 写入 `v.ganttConfig.rowHeight` 并 `saveConfig()` 持久化（走合法应用内保存通道，未篡改 `.multiview.json`）。

- **构建验证（#410）**：`node check-js-syntax.js`→JS syntax OK；`dotnet build -c Release`（C:/Program Files/dotnet/dotnet.exe）→0 错误（13 历史既有 warning，与本次无关）；手动 `rm -f MultiTableAddin/dist/files/MultiTableAddin.dll && cp` 更新交付（产物约 817664 字节）；`verify-build.py`（managed python3）全绿：#406×4（resolveVisualEditClose 分发 utf-16le / __mtPromptVisualEditClose 全局 / __mtDiscardVisualEdits 桥 / 简化文案「是否保存」）、#407×3（left resize 模式 / right resize 手柄元素 / resize 手柄 CSS）、#408×2（ganttCenterBarIfNeeded / `if(within) return` 守卫）、#409×3（行高 CSS 变量 / 由 cfg.rowHeight 注入 / 设置面板下拉）+ 旧 #400×2 #402×13 全 OK，无 FAIL。末次 `rm -rf .../bin obj` 清理，确认 dist 三交付物（dll / AddIn.xll 1198592 / AddIn64.xll 1204224）完好。
- **版本一致性修复**：`verify-build.py` 顶部注释此前误写 `v2.9.19 甘特交互优化`，现同步 `Models.cs` 升 `2.9.19`/`GanttInteractTune`/`2026-08-14`，并把 verify-build.py 版本检查串由 2.9.18/GalleryCircleGanttSave 改为 2.9.19/GanttInteractTune，使两者一致。
