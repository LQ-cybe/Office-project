# 2026-08-20 工作记录

## 视图右键菜单打磨（v2.9.38 延续，#487~#492 + #493 文档 + #494 构建）

改动文件：`MultiTableAddin/src/MultiTableAddin/Html/index.html`（6 处）、`verify-build.py`、`多维表插件使用帮助.md`、`项目开发记录.md`。

### 代码改动（index.html）
- **#487 残留视图可删**：`isUserView(v)` 增加 `viewId` 前缀兜底正则 `^(?:copy|gantt)-`（早期缺 `userView` 标记的残留视图按前缀判定为用户视图）。
- **#488 另存弹框输名**：`duplicateView(v)` 重写为弹 `.overlay` 模态输入框，预填建议名，确认后深拷贝 + `userView=true` + 切换 + `saveConfig()`。
- **#491 类型+序号命名**：新增 `nextCopyName(viewType)`，基名 = `VIEW_LABEL[type]`（已无「视图」二字），正则 `^基名(\d+)$` 取最大序号+1；旧 `uniqueViewName` 保留兼容图表旧调用。
- **#489 悬停全名**：`renderSidebar` 给视图项加 `it.title = v.viewName || VIEW_LABEL[...]`。
- **#490 去分隔线**：右键菜单 `items` 数组「另存新视图」下不再有 `{sep:true}` 独立元素。
- **#492 颜色规范**：功能区图标 `.sidebar-top .btn .ic`、视图切换 `.view-item .ic/.vname` 用 `--accent`；用户视图 `.view-item.user .ic/.vname` 用 `--text-2`（激活态仍蓝）。废弃旧 `.vname.user` 单类，改父级 `.view-item.user` 控制。

### 构建验证（#494）
- `node check-js-syntax.js` OK → `dotnet build -c Release` **0 错误** → DLL 覆盖 `dist/files/MultiTableAddin.dll`（**874496 字节，13:43**）→ `python verify-build.py` **530 OK / 0 FAIL / exit 0**。
- verify-build.py 新增：#487×1 / #488×3 / #489×1 / #490×2 / #491×2 / #492×5 正向守卫 + #490 负向守卫（旧 `{sep:true}` 移除）。
- **踩坑**：① JS 源码 `\\d` 两反斜杠在 DLL 资源里仍是两反斜杠，Python 检查串转义易错 → 优先用不含反斜杠的独有片段（如 `var re=new RegExp('^'+base+'`）做守卫；② `index.html` 是 CRLF，跨行检查串要写 `\r\n` 而非 `\n`，否则误报 FAIL。

### 文档（#493）
- `多维表插件使用帮助.md`：新增「§8 视图管理（左侧侧栏视图项右键菜单）」；版本标识近期变更补 #487~#492；日期升 2026-08-20。
- `项目开发记录.md`：新增「二十六、v2.9.38 视图右键菜单打磨」整节；头部版本号升 v2.9.38、累计任务补 #467/#487~#492。

## 图标深蓝 / 日历悬停对齐甘特 / 画册吸收看板分组拖拽（v2.9.39，4 项需求）

改动文件：`index.html`（8 处）、`Core/Models.cs`（2.9.38→2.9.39 / 日期 2026-08-19→2026-08-20）、`verify-build.py`、`多维表插件使用帮助.md`、`项目开发记录.md`（二十七节）。

### 代码改动（index.html）
- **需求1 图标深蓝**：新增 `--accent-dark:#173A8C`；`.sidebar-brand .dot`、`.sidebar-top .btn .ic` 改深蓝（不影响 #492 视图切换列表的 `--accent`）。
- **需求2 日历悬停对齐甘特**：新增 `calEventTooltip(cfg, r)`（标题 + 起止日期范围）；日事件 `ev.title`、月分段 `seg.title` 均改用，与 `ganttBarTooltip` 同构。
- **需求3 画册吸收看板拖拽**：分组模式复用 `enableKanbanDrag` + 分组列 `dataset.group` + `dropover` 高亮；画册单击仅 `selectRow`、不再 `showDetailPanel`（看板本体零改动）。

### 备份与还原
- 改动前备份 5 文件至 `.workbuddy/backup/2026-08-20-153707/`（index.html / dll / verify-build.py / 帮助 / 开发记录），并写「本轮改动重点说明与还原指南.md」。

### 构建验证
- `node check-js-syntax.js` OK → `dotnet build -c Release` **0 错误** → DLL 覆盖 `dist/files/MultiTableAddin.dll`（**875520 字节，15:45**）→ `python verify-build.py` **542 OK / 0 FAIL / exit 0**（本轮 +11 正向 +2 负向守卫；原 #492 功能区图标断言由 `--accent` 改为 `--accent-dark`）。

## 拖到末尾定位 / 图标着色修复 / 删看板视图（v2.9.40，#501~#504）

改动文件：`index.html`（9 处，上轮已完成并验证）、`Views/AddViewDialog.cs`、`Core/ViewConfigManager.cs`、`Core/Models.cs`（2.9.39→2.9.40）、`verify-build.py`。

### 代码改动
- **#501 拖到末尾**：`enableKanbanDrag` 的 `up` 中，拖到 `.kanban-cols` 容器范围内但未命中具体 `.kanban-col` 时（末尾空白），取 `allCols[allCols.length-1]`（最后一列=末尾分组）。
- **#502 图标着色修复**：根因——`.sidebar-brand .dot` 规则虽有但 DOM 从未创建该元素（死规则）；且"设置"按钮用 `⚙` emoji 不随 currentColor 变色。修复：① `renderSidebar` 注入真实 `.sidebar-brand` 元素（让既有 `.dot` 深蓝规则生效）；② "设置"按钮 `⚙` 改为 `icon('gear')` SVG 齿轮（受 `.sidebar-top .btn .ic{color:var(--accent-dark)}` 着色）。
- **#503 删看板视图 + 画册用看板图标**：C# `AddViewDialog` 选项去 `看板视图`；`ViewConfigManager.CreateDefaultConfig` 删"看板视图自动创建"块（保留 enum `ViewType.Kanban` 兼容旧配置）。JS：`renderKanban` 函数删除，`type==='Kanban'` 路由到 `renderGallery`（画册已吸收看板分组拖拽）；`VIEW_ICON.Gallery` 由 `'gallery'` 改 `'kanban'`（画册改用看板图标），`ICONS` 中 `gallery:` 整条改为 `gear:` 齿轮 SVG（供"设置"按钮）。

### 构建验证（#504）
- `node check-js-syntax.js` 首跑报 `Unexpected token '<'` → 定位 `ICONS.gear:` 缺失左引号（上轮 Python re.sub 替换 SVG 时丢 `'`）→ 补 `'` 后 OK。
- `dotnet build -c Release` **0 错误** → DLL 覆盖 `dist/files/MultiTableAddin.dll`（**868352 字节，16:21**）→ `python verify-build.py` **exit 0 / 0 FAIL**。
- verify-build.py：版本断言 2.9.39→2.9.40；新增正向守卫（#501 末尾分组 / #502 sidebar-brand 注入 + gear / #503 Gallery:'kanban' + Kanban→renderGallery 路由）+ 负向守卫（renderKanban 已删 / ⚙ 已移除）。
- **踩坑修正**：① 上轮 Python re.sub 脚本把 `index.html` 由 **CRLF 归一化为 LF**（原 v2.9.38 本节所记"CRLF"已失效）；故 verify-build 跨行守卫须用 `\n`——本轮 #490「另存项后无分隔线」正向守卫原为 `\r\n` 误报 FAIL，改 `\n` 后通过。**不要再写 `\r\n`**。② 负向守卫不能用裸 `kanban`（utf-16le）：死代码 `KanbanView.xaml` 的 BAML 资源恒成立 "kanban"，且 JS `VIEW_ICON` 保留 `Kanban:'kanban'`/`Gallery:'kanban'`，裸 `kanban` 永不缺失→误报 FAIL。看板删除改由"renderKanban 函数已删"负向守卫 + `Gallery:'kanban'` 正向守卫覆盖。

### 备份
- 改动前备份 5 文件至 `.workbuddy/backup/2026-08-20-1601/`（index.html / AddViewDialog.cs / ViewConfigManager.cs / Models.cs / verify-build.py）。

## 看板迁移 + 图标改主题蓝 + 删品牌文案 + 两份 MD 文档（v2.9.41，#505~#509）

用户实测 v2.9.40 后反馈：①看板视图还在；②功能区和窗口左上角图标仍黑；③设置按钮上方多了"多维表分析"要删；④要 UI 分析 MD；⑤要桌面独立软件 MD。

### 根因诊断（关键）
- **v2.9.40 DLL 确实已部署**：HTML 经 `NavigateToString(LoadEmbeddedHtml())` 从内嵌资源 `MultiTableAddin.Html.index` 每次新读（无磁盘缓存）；C# 窗口标题 `Text="多维表分析"`（HtmlMainForm.cs:81）即用户所说"窗口标题"，与 v2.9.40 注入的 `.sidebar-brand` 文案是两处——用户要删的是后者，故 v2.9.40 已生效。
- **图标"黑"真因**：`.sidebar-top .btn .ic{color:var(--accent-dark):#173A8C}` 规则正确、DOM `class="sidebar-top"` 匹配、SVG 用 `stroke="currentColor"`——色值确为 `#173A8C`，但 14px 小图标下深海军蓝肉眼近似黑；`.view-item .ic` 早已是 `--accent:#3370FF`（亮蓝，用户未抱怨）。
- **看板还在真因**：C# 自动建看板已删（v2.9.40），但用户测试工作簿的**持久化配置**里仍有 Kanban 视图，`renderSidebar` 的 `forEach` 不过滤 Kanban（只过滤 Chart），故侧栏仍显示"看板视图"条目；`renderContent` 的 reroute 只影响渲染不影响列表。

### 代码改动（index.html 4 处）
- **#506 看板迁移**：`ensureViewDefaults` 的 `forEach` 开头加 `if(v.viewType==='Kanban'){ v.viewType='Gallery'; if(v.viewName==='看板视图'||!v.viewName) v.viewName='画册视图'; changed=true; }`——遗留 Kanban 迁为 Gallery，默认名修正；`changed` 触发 `saveConfig()` 持久化（下次开不再有 Kanban）。
- **#508 图标改主题蓝**：`.sidebar-top .btn .ic` 由 `var(--accent-dark)` 改 `var(--accent)`（#3370FF，与视图列表图标一致、肉眼明显非黑）。
- **#507 删品牌文案**：移除 `renderSidebar` 里 v2.9.40 注入的 `.sidebar-brand` 块（dot + "多维表分析"），并删 CSS `.sidebar-brand` / `.sidebar-brand .dot` 两条死规则。保留 `icon('gear')` 齿轮（仍受新规则着色）。

### 版本与验证
- `Models.cs` 2.9.40→2.9.41（ReleaseDate 仍 2026-08-20，CodeName 仍 StablePath）。
- verify-build.py：版本断言 2.9.40→2.9.41；删旧正向守卫（brand .dot / brand 注入 / 旧 #492 功能区图标深蓝）；新增正向（功能区图标改主题蓝 / 看板 Kanban→Gallery 迁移）；新增负向（`.sidebar-brand{display:flex` / `brand.className='sidebar-brand'` / 旧 `.sidebar-top .btn .ic{color:var(--accent-dark);}` 已移除）。
- `node check-js-syntax.js` OK → `dotnet build -c Release` **0 错误** → DLL 覆盖 `dist/files/MultiTableAddin.dll`（**867840 字节，19:54**）→ `python verify-build.py` **exit 0 / 0 FAIL**。

### 文档（#505 / #509）
- `多维表插件UI布局与设计风格分析.md`：布局骨架 / CSS Token / 视图体系 / 组件库 / 交互模式 / 可复用资产 / 局限。
- `桌面版独立软件设计方案.md`：独立 `.mtdb`(SQLite) + Excel 导入导出（MiniExcel 入 / ClosedXML 出）+ `IDataAdapter` 抽象（ExcelDataAdapter/SqliteDataAdapter 共用上层）+ 迁移路径 + 单文件 EXE 打包 + 里程碑。

## 看板视图彻底下线 + 外部 MD 文档全量同步（v2.9.42，#510 + #515，5 项需求落地）

v2.9.40 已删 `renderKanban` 函数、v2.9.41 已迁移遗留 Kanban→Gallery；本轮（用户原话"删掉，注意：需要更新所有的文档、帮助信息等"）把**面向用户的文档**全部改写，去除"看板视图"作为独立可用视图的表述，仅保留历史变更摘要里"看板已下线"的说明性文字。代码层前轮已完成（#511 列宽自适应 / #512 表单复制新增 / #513 表格·甘特分组折叠 / #514 图标统一 --accent / #516 构建 565 OK·0 FAIL），本轮只动文档。

### 改写的 5 份产品面向文档
- **`多维表插件使用帮助.md`**（重点）：页眉"v2.9.38 及以上"→"v2.9.42 及以上"；`切换视图`行去"看板"；**删除整节 `### 3. 看板视图`**；画册节由"### 4"重编为"### 3"（措辞"按看板式水平列布局"→"按水平列布局"、"列与看板一致统一高度"→"各列统一高度"、分组拖拽说明改为"画册分组直接复用分组列拖拽机制"）；章节顺编号 日历5→4 / 甘特6→5 / 仪表盘7→6 / 视图管理8→7；表格·甘特分组各加"分组标签前 ▼/▶ 折叠展开"；保护规则"8 类"→"7 类"、命名规律删"看板1/2/3"；版本标识 2.9.39→2.9.42 + 发布日期 2026-08-20 + 新增 3 条变更摘要（看板下线 / 图标统一强调蓝 / 列宽自适应+复制新增+分组折叠），旧"界面图标改深蓝"注明 v2.9.39、v2.9.42 改强调蓝。
- **`软件设置与文件设置对照表.md`**：`当前活动视图`去"看板"；`分组字段`"看板、甘特等"→"甘特、画册（分组模式）等"；各视图专属配置表删"| 看板视图 | 卡片标题字段、分组字段… |"整行。
- **`多维表插件UI布局与设计风格分析.md`**：版本基线 v2.9.41→v2.9.42；`--bar`"看板/画册分组 8 色"→"画册分组 8 色"；`--accent-dark`说明改为"v2.9.42 起所有小图标统一改用 --accent，此变量仅留作标题/大块底色"；`ViewType`枚举删 Kanban 独立项、注明"Kanban 视图类型已于 v2.9.42 移除，遗留配置打开时自动迁移为 Gallery"；画册渲染行改为"`**分组模式复用分组列布局与拖拽机制**（enableKanbanDrag，单击仅选中不弹详情）"；"看板/画册列"→"画册分组列"。
- **`桌面版独立软件设计方案.md`**：基线"v2.9.41 视图体系"→"v2.9.42 视图体系"；导出"看板/画册导出为分组工作表"→"画册分组导出为分组工作表"；"看板/画册分组"→"画册分组"；迁移路径"viewType=Kanban 按 v2.9.41 规则"→"按 v2.9.40+ 规则…看板视图类型已于 v2.9.42 移除"。
- **`画册与看板视图结构说明.md`**（由"画册+看板"重写为"画册为中心"）：标题去"看板"、适用版本 v2.9.42（2026-08-20）；导语改"画册复用看板式 `.kanban-cols`/`.kanban-col` 列布局，并复用 `enableKanbanDrag` 分组拖拽机制"；1.6"看板布局"→"看板式列布局"、1.8 注5"分组复用看板布局"→"看板式列布局"；第二节标题"（画板分组）"、导图去"看板"；2.4 标题"看板+画册分组共用"→"画册分组"；代码注释删看板行号（ renderKanban 章节确已在代码删除，行号仅留画册分组）；第三节"看板视图分组拖拽"→"画册分组拖拽（看板式列布局）"、`.kanban-card`→`.gallery-card`、`selectRow+showDetailPanel`→`selectRow`；速查/排查表去 renderKanban 行、改"画册分组拖拽无反应（未开启分组）"。

### 故意保留不改的历史/第三方/代码结构文档（避免篡改历史）
`项目开发记录.md`（历史日志）、`参考资料/飞书与WPS多维表功能对比.md`（第三方）、`Excel插件方案可行性分析.md`、`扩展功能可行性分析.md`、`开发规划文档与技术路线.md`、`多维表视图功能分析.md`（早期历史）、`图标汇总/图标信息对照表.md`（`kanban.svg` 资产引用保留）、`视图切换结构设计.md`（代码结构描述仍含 `else if(type==='Kanban') renderKanban();`，属历史结构，未改）。

### 验证
- Grep 确认帮助文档无"当前视图"层面看板提及（仅历史变更摘要保留说明）；画册说明文档 Grep `renderKanban|看板视图|看板视图类型|看板本体` 返回"无匹配"；残留"看板"均指"画板式列布局/代码复用"，属准确技术描述。
- 日期对齐：帮助文档`最后更新`/`发布日期`、UI分析`版本基线`、画册说明`适用版本`统一为 `2026-08-20`（与系统当前日期及本记忆文件一致；前轮 DLL 构建时间 2026-08-21 系笔误，已回正）。

### 踩坑
- 多份文档 `Edit` 报 `File has been modified since read` / `EBUSY`：因外部 linter/用户进程锁定或自上次读后被改动 → 重新 `Read` 取精确文本后针对性重试，全部成功。
- 帮助文档首次编辑甘特重编号因 EBUSY 失败致编号缺 5（日历4/甘特仍6/仪表盘也6）→ 重读全文单独修正甘特 `### 6`→`### 5`，编号恢复连续。

