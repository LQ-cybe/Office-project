# 2026-08-19 工作日志

## v2.9.38（StablePath）交付
- 修复 #467：配置 `SourceFile` 由完整绝对路径改为仅存文件名（`Path.GetFileName`），涉及 ExcelAdapter.cs:185（补 `using System.IO;`）、ViewConfigManager.cs 8 处、HtmlMainForm.cs:753 SaveConfig。verify-build.py 加负向守卫（DLL 中不出现完整路径字面量）。
- 升版 2.9.38 / StablePath / 2026-08-19（Models.cs + verify-build 版本断言）。
- 文档：多维表插件使用帮助.md（画册两来源+圆形+分组滚动、配置保存位置/嵌入查看/移动失效 FAQ、版本标识）、软件设置与文件设置对照表.md（嵌入Excel层级、配置保存位置行、图片显示模式）。
- 新增 画册与看板视图结构说明.md（cardMeta 字段、渲染流程、图片两来源/两形状、分组vs不分组、共用 flex 高度链、看板 enableKanbanDrag 拖拽改分组全链路、参数表与排查）。
- 构建：node check OK → dotnet build 0 错误 → DLL 863744B 覆盖 dist → verify-build 全 OK / 0 FAIL。

## 看板分组色条文档补充
- 在 画册与看板视图结构说明.md 新增 2.4 节「分组列顶部分组色条」：色条用 `.kanban-col-head{box-shadow:inset 0 3px 0 0 var(--group-color,var(--border))}`（3px 硬编码），颜色由固定 8 色 `groupColors` 数组按分组序号 `idx%8` 取，经 `col.style.setProperty('--group-color', color)` 落地；另整列浅底 `color+'10'` + 同色边框。看板(行1931-1937) 与画册分组(行2102-2108) 共用。
- 现状澄清：颜色与高度（3px）目前均**非用户可配**（颜色自动取调色板、高度写死 CSS）；实现已用 CSS 变量解耦，做成可配很轻量（改 `setProperty` 取值即可换色，抽 `--bar-h` 即可调高）。待用户决定是否接入「视图设置」。
