# 2026-08-13 工作日志

## 13 项反馈修复收尾（版本 2.9.7，任务 #284–#296）

上轮已完成 index.html 内全部 13 项代码修复（#284–#295）。本轮为构建/交付/验证收尾（#296）。

### 构建与交付
- `dotnet build -c Release`（src/MultiTableAddin）：**0 错误**，7 个预存警告（与本次改动无关）。
- 产物 `bin/Release/net8.0-windows/MultiTableAddin.dll`（718848 字节）拷至 `MultiTableAddin/dist/files/MultiTableAddin.dll`（dist 已含 .xll 桩，交付完整）。
- `node check-js-syntax.js`：JS 语法 OK。

### verify-build.py 维护
- 修正 4 个因本次改字符串而失效的检查串：
  - #4 周视图今日列空规则：`.cal-day-col.today:not(.is-week){}`（原为带 background）
  - #6 图表标签阈值：`APP_SETTINGS.labelHideThreshold || 0`（原为 `cfg.minLabelValue`）
  - D5 选中高亮：`box-shadow:inset 0 0 0 2px #003366`（原为 `outline:2px solid #003366`，CSS 改名）
  - D7 批量编辑弹窗：`modal.style.maxWidth='1400px'`（原为 1200px）
- 新增 13 项修复检查串（#284~#295）+ 1 个负向检查（`cfg.minLabelValue` 不再作为标签阈值）。
- 重跑结果：**84 OK / 0 FAIL**（exit 0）。

### 13 项修复要点（已落地 index.html）
- #284 窗口默认最大化（APP_SETTINGS.openMaximized:true，localStorage 往返）
- #285 甘特今日线 2px→1px（与日历一致）
- #286 甘特季度标签"第一~第四季度" + 年切换按钮（prevY/nextY 改 anchorDate）
- #287/#288 图表标签阈值(labelHideThreshold)、汇总行位置(summaryPosition) 上移为全局设置，并入"设置"面板；删除各视图冗余 minLabelValue 控制
- #289 表格仅单击未改数据不提示"保存成功"（normCellVal 比对）
- #290 表单/日历查找结果面板（searchResults，位于右侧详情窗上方，updateRightCol 统一管理布局）
- #291 批量编辑字段窗口风格对齐主窗口（modal 1400px、列 minWidth + tableLayout:auto、控件等宽）
- #292 关闭月视图"单击任务切换视图"，仅日/周/月按钮切换
- #293 月视图横向拖动任务块实时跟随光标（transform:translateX 替代 ghost clone，规避 WebView2 transform 祖先干扰）
- #294 周/日视图选中任务高亮边框（box-shadow inset #003366）
- #295 日视图今日列不填充背景色（仅保留贯穿时间红线）

## 第二批 7 项反馈修复（任务 #297–#303，版本 2.9.7）

用户实测后再提 7 项问题，本轮全部落地并通过构建验证。

### 已修复
- **#297 查找结果窗口**：去掉"#1"这类序号前缀（搜索结果与日历列表统一）；搜索结果项改用日历风格的颜色左边框 + 标题·副标题格式。
- **#300 选中高亮**：月视图事件补 `data-row`，使 `selectRow` 的 `.cal-event[data-row]` 命中；并在 `selectRow` 清理列表加入 `.sr-item`，切换选中时同步清除搜索项高亮。保证"无论查找还是点击，全局仅一个高亮"。
- **#301 月视图每格任务数**：改为全局设置 `APP_SETTINGS.monthMaxEvents`（默认 5），设置面板新增"月视图每格显示任务数"数字框，实时 `saveAppSettings()+renderContent()`。
- **#298 日历拖拽重写（核心 bug）**：
  - **根因**：`makeMonthEventDraggable` 中 `cells.indexOf(cell)` 返回 -1——`cells` 是 `{out,d,date}` 普通对象数组，而传入的 `cell` 是 DOM 元素，indexOf 永远找不到。于是每次拖拽都从索引 -1 推算目标，把任务甩到错误/越界日期（甚至 filler 日期），表现为"拖动后任务消失 / 只是移动不是改日期"。
  - **修复**：不再依赖 cells 索引，直接按 `deltaDays = round(dx/格宽) + round(dy/格高)*7` 从任务真实起止日推算；左缘=调开始、右缘=调结束、中部=整体平移（时长不变）；横向按天、纵向按整周；自动按整天对齐；写入前校验日期合法性，绝不写非法值（防"消失"）。
  - 日视图新增 `layoutDayColumn`：同列时间冲突的任务自动水平并排（飞书语义：日视图不允许重叠）；周视图允许重叠（不并排）。
  - 同步更新 `多维表插件使用帮助.md` 的日历/甘特交互说明。
- **#299 详情面板时间不匹配**：详情面板的日期时间控件原为 `<select>`（仅 :00/:30/:59 选项），非整 30 分的分钟会被错误回退 → 时间显示错乱（测试数据多日期字段，显得"所有数据都对不上"）。改为原生 `<input type="time">`，任意 HH:MM 正确显示/编辑。
- **#302 批量编辑窗口样式**：控件此前无 `.cell-edit` 类，用默认 OS 样式，与主表不一致。统一加上 `cell-edit` 类（typeSel/step/max/min/opts/reqCb），并去掉固定 colgroup 改 `tableLayout:auto` 自动列宽。
- **#303 甘特年切换/季度空白**：
  - 季度视图 `ganttRange` 原固定取 `anchor.getFullYear()`，与数据年不一致时任务条落在可视窗口外→全空白。改为取数据中点年份（无数据才退锚点/当前年），始终可见。
  - 年切换 ◀▶ 按钮原仅在季度视图出现，现**所有视图常驻**。
  - `ganttStep` 补 `'year'` 分支（年切换每年一个刻度）。

### 构建与验证
- `dotnet build -c Release`（注意：实际工程路径为 `MultiTableAddin/src/MultiTableAddin`；dotnet 在 `C:/Program Files/dotnet/dotnet.exe`，受管 dotnet 路径不存在）：**0 错误**，7 预存警告。
- DLL 721920 字节拷至 `dist/files/MultiTableAddin.dll`（先 `rm -f` 再 `cp`，规避 Excel 句柄锁 Permission denied）。
- `node check-js-syntax.js`：JS 语法 OK。
- **verify-build.py 维护**：修正 2 个因改字符串失效的检查（C2 `tSel`→`tInp`、#293 `translateX`→`translate`），新增 11 个检查串（#303×3、#298×6、#299×1、#302×2）。重跑 **全部 OK / 0 FAIL**（exit 0）。
- 当前 DLL 版本串仍为 2.9.7 / 2026-08-13 / UXPolish（未升版，属同版本内反馈修复）。

## 第三批 14 项反馈修复（任务 #305–#319，版本 2.9.7）

用户重建插件后又实测提出 14 项（#3、#12 各重复一次），本轮全部落地并通过构建验证。

### 已修复（index.html）
- **#305 仪表盘标签切换不完整**：Line/Area 分支多余的 `groups.some(...)` 包裹导致部分图表标签不响应，改为统一 `if(cfg.showLabels!==false){}`。
- **#306 甘特左上布局**：重构 `renderGantt`——冻结左列 + 可横向滚动画布；左上角对标日历布局 `◀ 区间 ◶`（`rangeLbl` + `prevBtn◀/nextBtn▶`），新增 `ganttStepAnchor(dim,±1)` 按维度步进锚点并重绘。
- **#307 甘特年切换**：`ganttRange/ganttUnitStart/ganttTickLabel` 补 `year` 分支（5 年居中窗口、1 月 1 日起、整年标签）；右上角季按钮后加"年"按钮（所有视图常驻）。
- **#308 甘特新建默认时长**：`addRow` 对甘特有 `startField` 的视图，开始=当前时间（对齐:00/:30）、结束=开始+`defaultDurationHours`（默认 4）；该值入甘特配置窗口"默认时长(小时)"数字框（min0 step0.5）；配置窗复选框统一左对齐（label `justify-content:flex-start`）。
- **#309 月视图横向拖拽无效**：`makeMonthEventDraggable` 改为按 `relX` 相对宽度三分（`relX<w/3` 调起、`>2/3` 调止、中间平移），左右缘真正改日期而非仅移动。
- **#310 图表设置复选框左对齐 + #311 配色主题高亮蓝色**：showWrap/chShowWrap 复选框 `justify-content:flex-start`；新增 `::selection{background:var(--accent);color:#fff;}` 替换灰色。
- **#312 查找结果定位+高亮**：`renderSearchResults` 去掉 `Form/Calendar` 限制，单击统一 `selectRow(rowIndex)` + `scrollIntoView`，高亮改画廊选中风格（`.cal-day-event.selected`/`.cal-event.selected` 用 `var(--accent)` 边框+2px 阴影）；Form 视图同时切到对应记录。
- **#313 仪表盘总设置窗口**：仪表盘视图栏加"总设置"按钮，`showDashboardTotalSettings` 以表格批量编辑全部图表（标题/类型/维度/度量/聚合/时间维度/高度/显示标签），"应用"一次写入 `v.dashboardConfig.charts`。
- **#314 批量编辑窗口加宽**：`modal.style.maxWidth='2000px'`；表头列 minWidth 字段名 160 / 类型 120 / 选项 240；按字段类型禁用步进/最大/最小（文本等无数值语义的类型置灰）。
- **#315 还原时间控件**：`buildDateTimeSplitInput` 还原为 `<select>`（:00/:30/23:59），`readDateTimeSplitInput` 据此回读——撤回上次误改的原生 `<input type=time>`。
- **#316 删除日历添加按钮**：`renderCalendar` 移除 `cal-add` 按钮及其 CSS 规则。
- **#317 甘特今天按钮**：右上角加回"今天"(`id=ganttToday`)，点击 `anchorDate=new Date()` 重绘定位今天。
- **#318 甘特水平滚动条**：展示区画布 `.gantt-canvas{overflow:auto}` 仅图表区横向滚动（独立平移，符合 `甘特图图表区独立横向平移.md`）。

### 构建与验证
- `dotnet build -c Release`（路径 `MultiTableAddin/src/MultiTableAddin`，dotnet=`C:/Program Files/dotnet/dotnet.exe`）：**0 错误**，仅预存 CS 警告。
- DLL 732160 字节拷至 `dist/files/MultiTableAddin.dll`（先 `rm -f` 再 `cp`，规避 Excel 句柄锁）。dist 与 build 两份产物时间戳/字节一致。
- `node check-js-syntax.js`：JS 语法 OK。
- **verify-build.py 维护**：修正 3 个因改字符串失效的检查（C2 `tInp`→`tSel`、#286 `prevY/nextY`→`year` 分支、`#294/#303` 画廊高亮串）；新增 14 项检查串（#305~#318 各功能锚点 + 负向 `cal-add`）。重跑 **118 OK / 0 FAIL**（exit 0）。
- 当前 DLL 版本串仍为 2.9.7 / 2026-08-13 / UXPolish（同版本内反馈修复，未升版）。

## 第四批 2 项 bug 修复（任务 #320–#321，版本 2.9.7）

用户实测反馈 2 个 bug，本轮定位修复并通过构建验证。

### 已修复
- **#320 表单视图渲染报错 `Cannot read properties of undefined (reading 'addEventListener')`**：
  - 根因：#315 把时间控件还原为 `<select>` 版 `buildDateTimeSplitInput` 时，只设了 `wrap._dateInput` 与 `wrap.tSel`，漏掉 `wrap._timeInput`；而 `renderForm` 第 1240 行 `ctl._timeInput.addEventListener('focus', onf)` 访问 undefined 抛错，导致整个表单视图渲染中断。
  - 修复：`buildDateTimeSplitInput` 补 `wrap._timeInput=tSel;`（时间下拉也联动焦点→右侧字段设置）。
- **#321 查找结果单击定位+高亮仍无效**：
  - 根因：旧点击处理仅 `selectRow`+滚动+开右侧详情，但日历/甘特中搜索到的任务若不在当前显示的月份/区间，元素根本未渲染，高亮与滚动都找不到目标；且日历无"跳转到任务日期"逻辑，故左侧视图纹丝不动。表格视图因 `data-row` 存的是 JSON（`{"rowIndex":N}`），滚动选择器 `[data-row="N"]` 也匹配不到。
  - 修复：新增 `locateInCurrentView(rowIndex)`，按 `activeViewType()` 分视图处理——
    - 表单：切到该记录；
    - 日历：读 `calendarConfig.dateField` 取任务日期，`calState` 设为目标年月日并重渲染 `renderCalendar()`，再 `selectRow` 高亮 + `scrollIntoView`；
    - 甘特：读 `ganttConfig.startField` 取开始日设 `ganttState.anchorDate` 并重渲染 `renderGantt()`，再高亮+滚动；
    - 表格：按 `JSON.parse(tr.dataset.row).rowIndex` 找到对应行，`selectRow(rowIndex, target)` 高亮+滚动（修正 JSON data-row 匹配）；
    - 看板/画册：`selectRow` 高亮 + 滚动到卡片。
    - 末尾统一 `renderSearchResults()` 刷新查找列表、使被定位项也显示选中态。
  - 同时删除 `renderSearchResults` 内已无用的 `var type=activeViewType();`。

### 构建与验证
- `dotnet build -c Release`（路径 `MultiTableAddin/src/MultiTableAddin`）：**0 错误**，仅预存 CS 警告。
- DLL 733696 字节拷至 `dist/files/MultiTableAddin.dll`（先 `rm -f` 再 `cp`，两份产物一致）。
- `node check-js-syntax.js`：JS 语法 OK。
- **verify-build.py 维护**：新增 3 项检查串（#320 `_timeInput=tSel`、#321 `locateInCurrentView`、`calState.year=dv.getFullYear()...`、`ganttState.anchorDate=sv`）。重跑 **全部 OK / 0 FAIL**（exit 0）。

## 第五批 9 项反馈修复（任务 #326–#330，版本 2.9.7）

用户实测再提 9 项（#1/#2 复核 #320/#321 已确认在 dist DLL 生效；#3–#9 本轮新修）。全部落地、构建验证、verify 全绿。

### 已修复（index.html）
- **#1/#2 复核表单渲染 + 查找定位**：确认 `wrap._timeInput=tSel` 与 `locateInCurrentView` 在 dist DLL（736256 字节）中均已生效。
- **#3 仪表盘总设置列太窄**：`showDashboardTotalSettings` 表头改用 `DASH_COLS` 数组带 `minWidth`（标题110/类型90/维度120/度量120/聚合90/时间100/高度70/显示标签80），各控件 `width:100%` 撑满单元格。
- **#4 甘特滚动与布局（双栏冻结重构）**：将单容器 `.gantt-canvas{overflow:auto}` 改为真·双栏——左 `.gantt-frozen`（独立面板：fz-head 列头 + fz-body 行标签/分组名，永不横向滚动）＋ 右 `.gantt-scroll{overflow:auto}`（仅此区横向滚动，水平滚动条只覆盖轨道区，左表不再被盖）。分组名移入冻结列 `.gantt-group-fz`（固定左侧、不随滚动变化）。左/右两栏行高严格对齐（行 30px、分组 28px、列头 26px），并用 `fzBody<->scroll` 双向 `scroll` 事件同步垂直滚动（`syncing` 锁防回环）。今日标记线统一 **2px**（主规则 + `.gantt-overlay .gantt-today-line` 两处均改，消除不同视图粗细不一致）。
- **#5 复选框左对齐**：根因 `.setting-row input{flex:1}` 把复选框拉伸覆盖 `width:16px`。新增 `.setting-row input[type=checkbox]{flex:0 0 auto;width:16px;height:16px;margin:0;}`（特异性高于通用规则），甘特配置"仅计算工作日"、图表设置"显示标签"复选框均左对齐。
- **#6 查找结果仅日历/表单**：`renderSearchResults` 开头 `if(vt!=='Calendar' && vt!=='Form'){ box 关闭并清空; return; }`。
- **#7 甘特"下一段"间距**：`rangeLbl` 由 `flex:1` 改为 `flex:0 0 auto`，按钮紧贴日期（不再被撑到最右）。
- **#8 日历箭头**：`‹/›` → `◀/▶`（`calPrev`/`calNext`），布局与功能不变。
- **#9 甘特步进对齐日历**：确认 `ganttStepAnchor(dim,±1)` 已按当前维度步进（日±1天/周±7天/月±1月/季±3月/年±1年），与日历 `moveCal` 按视图步进同语义，无需改代码，仅同步校验串。

### 构建与验证
- `dotnet build -c Release`（路径 `MultiTableAddin/src/MultiTableAddin`，dotnet=`C:/Program Files/dotnet/dotnet.exe`）：**0 错误**，仅预存 CS 警告。
- DLL 736256 字节拷至 `MultiTableAddin/dist/files/MultiTableAddin.dll`（先 `rm -f` 再 `cp`；dist 在 `MultiTableAddin/dist/files/`，非仓库根 `dist/`）。
- `node check-js-syntax.js`：JS 语法 OK。
- **verify-build.py 维护**：#285 今日线检查串 1px→2px；#305 `gantt-canvas`→`gantt-scroll`；新增 13 项检查（#326 DASH_COLS、#327 gantt-body/frozen/scroll/fz-row/group-fz/垂直同步/今日线2px、#328 复选框规则、#329 仅日历表单、#330 日历◀▶/rangeLbl 紧凑/stepAnchor）。重跑 **135 OK / 0 FAIL**（exit 0）。

## 第六批 15 项反馈修复（任务 #331–#343，版本 2.9.8，含 verify-build.py 收尾）

用户实测再提 15 项（#1 仪表盘总设置加宽、#2/#4 甘特日/周/月不再缩短/仅季年滚动、#3 甘特◀▶平移语义+移除数据回跳、#5 删日历字段开关、#6 日历默认时长、#7 单击方格按默认时长、#8 月视图周一..周日表头、#9 查找列表统一；分开连接、#10 设置=全局长期生效、#11 设置窗口统一圆角 modal、#12 周起始日、#13 月视图左右拖拽修复、#14 批量编辑加宽、#15 同日期起止不再整视图空白）。全部落地、构建验证。

### 已修复（index.html）
- **#1 仪表盘总设置**：`showDashboardTotalSettings` 弹窗 `width=1200px`、wrap `max-height:60vh;overflow-y:auto;overflow-x:hidden`；`DASH_COLS` 标题列 220（其它按比例）；`tableLayout='fixed'` + 百分比列宽 → 取消水平滚动。
- **#2/#4 甘特 fit-to-width**：`ganttFit=(dim==='day'||'week'||'month')` 时 `canvasW=null`（充满容器宽），仅 `quarter/year` 用 `GANTT_DAY_PX`（12/4）固定像素；`scroll.style.overflowX=(canvasW==null?'hidden':'auto')`，消除视图"变短一节"+非必要水平滚动条。
- **#3 甘特 range 重写 + 平移语义**：`ganttRange` 以 `anchor||now` 为中心——年视图 `±2 年`、季视图 `qm..qm+12`（滚动 4 季窗口）、日/周/月 `±span/2`（span=3/21/70 天）。删除"窗口不在数据区就回跳数据中点"逻辑（那会让 ◀/▶ 平移被立刻重置、看似无效）；`anchorDate` 初值：今天在数据区间外时取数据中点。
- **#5 删日历字段开关**：`renderCommonTools` 条件改 `if(type!=='Chart' && type!=='Calendar')`。
- **#6/#7/#12 日历默认时长 + 周起始日**：`calDefaultDurationMinutes`（默认 30，全局 `APP_SETTINGS`）；`calDraftValues(cfg,date,h,m)` 生成 开始=基准时间、结束=+默认时长 的草稿（新增时开始=当前时间）；`showDraftDetailPanel` 预览。月视图表头 `weekDayNames()`（周一..周日，随周起始日变首列）；`weekStartIndex()/weekColOf()` 辅助；周视图列头按真实日期 `周日..周六 + N日`。
- **#8 月视图表头**：`cal-dow` 改为与周列头一致（居中 30px、600 字重、12px）。
- **#9 查找列表统一**：新增 `searchHitText(row,kw)` 用 `；` 连接命中字段值（未命中也回退首个可见值），表单/日历共用。
- **#10/#11 全局设置 modal 化**：`APP_SETTINGS` 存 `localStorage['mt_app_settings']`（与文件无关，长期生效）；`toggleSettingsPanel` 改为 `#appSettingsOverlay` 圆角 `.modal`（标题"全局设置"），删旧 `.settings-panel` CSS。
- **#13 月视图拖拽修复**：`makeMonthEventDraggable` 边缘判定改绝对像素 `EDGE=8`（原 1/3 宽度法在窄格失效）；落点用 `elementFromPoint` 取 `.cal-cell.dataset.date` 精确定位（cell 已写 `dataset.date`）。
- **#14 批量编辑加宽**：`showBatchFieldEditor` `width=1040px`、wrap `overflow-x:hidden`、`BATCH_COLS`（步进/最大/最小各 135、选项 120），`tableLayout='fixed'`。
- **#15 日期容错**：甘特 `renderGantt` 行级校验——缺开始/缺结束/结束早于开始 → 进 `badRows` 并顶部红色 `.view-err-bar`（列出错误行号），**仅无效行跳过，有效行照常绘制**（同日期起止=零时长=有效）。日历同理加 `calBad` 错误条。

### 构建与验证
- `dotnet build -c Release`（路径 `MultiTableAddin/src/MultiTableAddin`）：**0 错误**，仅预存 CS 警告。
- DLL 744960 字节拷至 `MultiTableAddin/dist/files/MultiTableAddin.dll`（先 `rm -f` 再 `cp`）。
- `node check-js-syntax.js`：JS 语法 OK。
- **verify-build.py 维护（收尾）**：本轮重构甘特 range / 批编辑器布局使 6 个旧检查串失效（C3 旧 `quarter:90`、C9 `inView` 回跳、D7 `2000px`、#286 旧年分支注释、#291 `auto`、#303 数据年中点）→ 全部改为指向新代码串（C3 `span={day:3,week:21,month:70}[dim]||70`、C9 `var yy=a.getFullYear()`、D7 `width='1040px'`、#286 `min:new Date(yy-2,...),max:new...yy+2`、#291 `tableLayout='fixed'`、#303 `qm+12` 季度滚动窗口）；新增约 52 项 #331–#343 正向 + 5 项负向。重跑 **全部 OK / 0 FAIL**（exit 0）。
- **关键经验**：改 HTML 字符串后必须同步 `verify-build.py` 检查串；凡引用被重构代码（如旧 range 分支、旧 2000px/auto 布局）的旧检查，要么改为新串、要么移入 negative_checks，否则校验会假性 FAIL。

## 第七批 6 项反馈修复（任务 #344–#349，版本 2.9.8）

用户实测再提 6 项（批量编辑字段改不了 / 甘特仅年季有滚动 / 全局设置改名+月视图任务数迁移 / 标签阈值控件迁入仪表盘 / 日期列误报已保存 / 月视图横向拖拽无效）。全部落地、构建验证、verify 全绿。

### 已修复（index.html）
- **#344 批量编辑字段属性窗口增强**：`showBatchFieldEditor` 由 7 列扩至 12 列——新增「显示格式 / 最小长度 / 最大长度 / 正则验证 / 错误提示」。`BATCH_COLS` 改为 `[字段名130,必填48,类型96,步进值84,最大值84,最小值84,显示格式120,最小长度84,最大长度84,正则验证120,错误提示120,选项126]`（合计 1180px）；`modal.style.width='1200px'`（原 1040）；wrap `overflow-x:auto`、table `width=batchTotalW+'px'`（横向可滚）。`updateCols()` 按类型启用/禁用：步进/最大/最小仅数值；显示格式仅文本/数值/日期类；最小/最大长度仅文本类；正则/错误提示仅文本与数值类；选项仅 Select/Quarter。`apply` 经 `syncFieldToConfig` 写入 `format/minLength/maxLength/regex/errorMessage`。
- **#345 甘特全维度横向滚动**：移除 `ganttFit`（日/周/月"充满容器宽"）特例，所有维度统一 `canvasW=Math.round(totalDays*GANTT_DAY_PX)`（像素 `day:80,week:40,month:16,quarter:12,year:4`），`scroll.style.overflowX='auto'` 恒定启用——内容超宽即出现横向滚动条（周/月视图现在真正可横向平移）。
- **#346 全局设置改名 + 月视图任务数迁移**：`toggleSettingsPanel` 标题"全局设置"→"软件设置"，提示"以上为软件全局设置"→"以上为软件设置"；移除"月视图每格显示任务数"控件，改为在 `showCalendarSettings` 新增"月视图最大任务数"（写 `APP_SETTINGS.monthMaxEvents`，实时 `saveAppSettings()+renderContent()`），提示同步增列该参数。
- **#347 仪表盘"总设置"→"图表设置" + 标签阈值控件迁移**：仪表盘工具条"总设置"按钮→"图表设置"（其弹窗 `showDashboardTotalSettings` 标题亦改"图表设置"）；把"隐藏≤该值的图表标签"控件从软件设置面板迁出，作为 `thInp`（`id=setLabelThreshold`）置于仪表盘工具条"图表设置"按钮之后（`隐藏≤[数字框]标签` 行内控件），写 `labelHideThreshold`。
- **#348 表格日期列误报"已保存"**：`editCell` 的"未改动即跳过"比较由 `normCellVal` 裸串比较改为类型感知的 `cellValueEqual(f,val,raw)`——日期按 `toDate`+`isSameDay`（DateTime 用毫秒）；数值按 `Number`（空串→null 等价）。避免 `"2026-08-13"` vs `"2026-08-13T00:00:00"` 误判为已修改而误弹"已保存"。
- **#349 月视图横向拖拽调结束日期无效（核心 bug）**：根因有二——
  1. 拖拽时 `ev` 置 `zIndex=100` 且 `pointer-events` 未关，导致 `elementFromPoint` 命中 `ev` 自身 → `.closest('.cal-cell')` 永远返回原格 → `deltaDays` 恒为 0。
  2. `resize-end` 分支 `ne=origEnd+deltaDays`（deltaDays 是相对起点的偏移）错误地加在结束日上，落点错乱。
  - 修复：拖拽开始设 `ev.style.pointerEvents='none'`（落点穿透），`onUp` 用 `cellDate`（`elementFromPoint` 命中的落点格日期）直接作为目标；`resize-end: ne=cellDate`（落点格即新结束日），`resize-start: ns=cellDate`，`move: ns/ne` 同步偏移；兜底（移出网格）仍按 `moveDelta=dCols+dRows*7`。并在 `state._calDragMoved` 置位以抑制拖拽结束后的落点格误点击（避免误开草稿详情）。

### 构建与验证
- `dotnet build -c Release`（路径 `MultiTableAddin/src/MultiTableAddin`，dotnet=`C:/Program Files/dotnet/dotnet.exe`）：**0 错误**，仅预存 CS 警告。
- DLL 750080 字节拷至 `MultiTableAddin/dist/files/MultiTableAddin.dll`（先 `rm -f` 再 `cp`，规避 Excel 句柄锁）。
- `node check-js-syntax.js`：JS 语法 OK。
- **verify-build.py 维护**：修正/新增检查——D7 与 #341 `width='1040px'`→`'1200px'`；#287/`#288` `ltInp`→`thInp`；#298 `deltaDays`→`moveDelta`；#305 `canvasW=ganttFit?null:...`→`canvasW=Math.round(...)`；#308 `总设置`→`图表设置`；#332 `ganttFit` 定义/overflowX `hidden:auto`→`'auto'`、GANTT 像素映射补全 `day/week/month`；#337 提示"软件设置"；#340 标题"软件设置"；#341 BATCH_COLS 12 列新串；负向检查 `#345 gantt fit-to-width(ganttFit)已废弃`（`var ganttFit=(dim==='day'` 已消失）。新增 #344 新列串、#346 月视图最大任务数、#347 隐藏≤、#348 `function cellValueEqual`、#349 `savedPE`/`ne=cellDate`。重跑 **188 OK / 0 FAIL**（exit 0）。
- **关键经验**：`negative_checks` 也会因重构失效——旧 `#332 gantt no unconditional px canvas width`（断言 `var canvasW=Math.round(totalDays*GANTT_DAY_PX)` 不存在）在 #345 让所有维度改用该写法后变成"假阳性 FAIL"，须同步改为断言已废弃的 `ganttFit` 不复存在。

## 第八批 甘特图表区独立横向平移（任务 #350，版本 2.9.8）

依据设计文档 `甘特图图表区独立横向平移.md`，把甘特从"按维度居中窗口"重构为"全量缓冲时间轴 + 右侧独立横向平移 + 自定义底部滚动条"。

### 已修复（index.html）
- **核心模型重写**：`renderGantt` 不再用 `ganttRange(dim,...)` 取"居中小窗口"作为可视范围，改为 `ganttScrollTotal(dim,dataMin,dataMax)` 取**滚动条总范围**（≠任务最小/最大日期）：任务区间 + 25%（至少 7 天）缓冲，再按当前粒度贴合自然刻度（`floorToTick`/`ceilToTick`：周一/每月1日/季度首日/每年1月1日/当日）；无有效任务时用"今天 ±6 个月"。轨道按 `canvasW=totalDays*GANTT_DAY_PX` 全量绘制 → 整条时间轴可横向滚遍。
- **自定义底部横向滚动条**：新增 `.gantt-hscroll`（track+thumb），`margin-left=totalLabelW` 对齐轨道区；滑块宽度/位置随 `scroll.scrollLeft` 实时更新；**时间轴未超出可视区域时 `display:none` 自动隐藏**（按需渲染）。原生横向滚动条 CSS 隐藏（`.gantt-scroll{scrollbar-width:none}` + `::-webkit-scrollbar{...display:none}`）。
- **画布空白处拖拽平移**：`ganttScrollEl` 上 `mousedown` 且非 `.gantt-bar` 时按 `scrollLeft=startLeft-(dx)` 平移（进度条自身 `stopPropagation` 不触发）。
- **顶部 ◀/▶ 改为翻页**：按 `clientWidth` 整页平移（上一页/下一页），不再改 `anchorDate`；"今天"按钮改调 `ganttCenterOnDate(new Date())`（纯视口居中，不重绘）。
- **切换粒度保留视口时间位置**：`ganttSwitchDim(d)` 先记 `ganttCurrentCenter()`（当前视口中心日期），再换 `dim` 并重绘，重绘后 `ganttCenterOnDate(anchorDate)` 居中到同一日期——**禁止跳回时间轴起点**。
- **顶部期间文本实时化**：`ganttUpdateRangeLabel()` 随横向滚动更新 `rangeLbl` 为当前可见区间（日视图显示具体日期、年视图显示年份、其它显示 年-月）。
- 全部交互仅平移视口，**绝不修改任务原始日期数据**。
- 既有 `ganttRange`/`ganttStepAnchor` 函数保留未删（仅不再被可视范围调用），以保证 verify-build 既有检查串（C3/C9/#303/#333/#305/#330）继续有效。

### 构建与验证
- `dotnet build -c Release`（路径 `MultiTableAddin/src/MultiTableAddin`，dotnet=`C:/Program Files/dotnet/dotnet.exe`）：**0 错误**，仅预存 CS 警告。
- DLL 759296 字节（750080→759296，+9KB，对应新增代码）拷至 `MultiTableAddin/dist/files/MultiTableAddin.dll`（先 `rm -f` 再 `cp`）。
- `node check-js-syntax.js`：JS 语法 OK。
- **verify-build.py 维护**：新增 14 项 #350 正向检查（ganttScrollTotal/floorToTick/ceilToTick/±6月默认窗/gantt-hscroll 类/hscroll-thumb 类/ganttCenterOnDate/ganttSwitchDim/ganttUpdateRangeLabel/拖拽平移串/翻页串/隐藏原生滚动条 css+webkit）；新增 1 项负向检查（"`var range=ganttRange(dim, ganttState.anchorDate`" 已不再出现）。重跑 **202 OK / 0 FAIL**（exit 0）。
- **关键经验**：`ganttRange` 这类"被取代但仍有 verify 检查锚点"的函数，**不要删除**，令其在文件内保留定义即可；只把"调用点"移除，并把对应 verify 检查从"调用点串"迁到"函数定义串"或加负向检查。

## 第九批 6 项反馈修复（任务 #352–#357，版本 2.9.7 未升版）

用户实测后再提 6 项问题（日视图标贴/批量类型列窄/百分比步进值未生效/甘特今日线变粗/月视图横向调整无效/甘特显示边界双模式）。已全部落地并通过构建验证。

### 已修复（index.html）
- **#352 甘特日视图顶部标贴**：`ganttUnitStart` 日视图改为按"天"对齐（`setHours(0,0,0,0)`）；`ganttStep` 日视图每天一个刻度（`setDate(getDate()+1)`）；`ganttTickLabel` 日视图显示"周X M/D"——不再按每小时显示挤成一团。
- **#353 批量字段属性窗口"类型"列加宽**：`BATCH_COLS` 类型列 `96`→`124`px（合计 1180→1208px，modal 宽度未变）。
- **#354 步进值真正生效**：根因是 `buildFieldInput` 把 `input.step` 写死（`'0.01'`/Integer `'1'`），且 `step` 从未被 `applyFieldOverrides`/`getFieldOverride`/`syncFieldToConfig` 存读。修复：① `buildFieldInput` 中 Percentage 与数值字段的 `input.step` 取 `f.step`（百分比按"百分点"计，step=10 即 +10%）；② 三处持久化补齐 `step` 的读/写（`applyFieldOverrides` 回写 `r.step`、`getFieldOverride` 回写、`syncFieldToConfig` 写 `ov.step`）。表单控件、详情窗口的上下箭头现按字段配置步进。
- **#355 甘特今日标识线粗细对齐**：`.cal-current-line`（日历）与 `.gantt-today-line`（甘特主体+overlay）全部 `width/height:2px` + `box-sizing:border-box`，红 `#F53F3F`，两处视觉粗细一致（此前甘特线更粗）。
- **#356 月视图横向调整（拖拽改起止日期）**：参考 `任务条左右拉伸失败参考资料.md`（GanttWPF/angular-main 为 WPF 项目，非本 WebView2 DOM 技术栈，仅借鉴"左缘改开始/右缘改结束/主体平移"三模式思想 + 像素↔日期换算）。
  - 事件渲染处新增左右 `ew-resize` 手柄 `.cal-event-resize-l/.r`（宽10px），各自 `mousedown` 直接以 `resize-start`/`resize-end` 调 `monthDragStart`，逻辑清晰不被主体拖拽干扰。
  - 重构：`makeMonthEventDraggable` 抽出 `monthDragStart(ev,r,cfg,cell,mode,e)`（保留 pointer-events:none 穿透 + `elementFromPoint` 命中 `.cal-cell.dataset.date` 落点定位；resize-start/resize-end/move 三模式）；主体 `mousedown` 跳过 `.cal-event-resize` 句柄（句柄已自行处理）。仅配置了 `endDateField` 才允许左右拉伸。
- **#357 甘特显示边界双模式设置**：`ganttScrollTotal` 重写——新增 `cfg.boundaryMode`（默认 `'feishu'`）+ `cfg.boundaryExpandDays`（默认 10）。
  - 飞书模式（feishu）：任务区间 + 25%（至少7天）缓冲，贴合自然刻度（当前行为不变）。
  - 项目边界外扩（project）：`lo=dataMin - expandDays天`，`hi=dataMax + expandDays天`，再贴合刻度。例：起 2026-1-3 / 止 2026-12-7，N=10 → 2025-12-24 ~ 2026-12-17。
  - 甘特配置弹窗 `showGanttConfig` 新增「显示边界模式」下拉（飞书模式/项目边界外扩天数）+「外扩天数」数字框（仅 project 模式显示）；保存写入 `v.ganttConfig.boundaryMode/boundaryExpandDays`。`ensureViewDefaults` 甘特默认配置补 `boundaryMode:'feishu',boundaryExpandDays:10`。

### 构建与验证
- `dotnet build -c Release`（路径 `MultiTableAddin/src/MultiTableAddin`，dotnet=`C:/Program Files/dotnet/dotnet.exe`）：**0 错误**，仅预存 CS 警告。
- DLL 763392 字节（759296→763392，+4KB）拷至 `MultiTableAddin/dist/files/MultiTableAddin.dll`（先 `rm -f` 再 `cp`，规避 Excel 句柄锁）。dist 与 src 字节数/时间戳一致（20:31），无陈旧 dist。
- `node check-js-syntax.js`：JS 语法 OK。
- **verify-build.py 维护**：首跑 220 OK / 5 FAIL（exit1）——失败项是前批次 `#285/#327/#339/#341` 遗留检查串，因本次 #355（加 `box-sizing:border-box`）、#353（类型列96→124）、#356（重构月拖拽剔除 `var EDGE=8`/`var canResize`）而字面子串不再匹配，**属合法改动致旧检查失效，按惯例同步检查串而非回退代码**。修正 5 个旧检查串（#285/#327 加 `box-sizing:border-box`；#339 用新 resize 手柄判定串替换 `var EDGE=8`/`var canResize=!!cfg.endDateField && w>=30`；#341 类型列 `96`→`124`），删重复 #355 gantt 行。新增 #352–#357 正向检查 + 负向检查（`n.setHours(n.getHours()+6)`/`return pad(t.getHours())+':00'`/`input.step='0.01'`/`var canResize=!!cfg.endDateField && w>=30`）。重跑 **224 OK / 0 FAIL**（exit 0）。
- 版本未升版，仍为 **2.9.7 / 2026-08-13 / UXPolish**（同版本内反馈修复）。
- **关键经验**：前批次的检查串是"脆弱锚点"——只要后续合法改动（加 CSS 属性、改列宽、重构提取函数）改动其字面子串，就会转 FAIL；维护规则是**同步更新检查串到当前真实文本 + 必要处加负向检查**，绝不为了 Verify 绿而回退业务代码。
