# 2026-09-01 工作记录

## 功能区按钮更名 + 提交用更新记录（#531 补丁）
- `Resources/Ribbon.xml` 第 6 行：功能区「打开」按钮 `label="多维表"` → `label="多维视图"`（tab `多维表`、group `多维表分析` 名称不动，故 verify-build 的 `label="多维表"` 守卫仍由选项卡命中、保持通过）。
- 重建 0 错误；DLL 复制到 `MultiTableAddin/dist/files/MultiTableAddin.dll`（876544 字节）；`python 脚本与工具/verify-build.py` EXIT=0 全 OK。
- 更新记录文档：`更新记录_甘特对齐图标配色与按钮更名.md`（项目根），汇总三项：①按钮更名 ②甘特月/年右侧空白 BUG（根因 canvasW=totalDays×GANTT_DAY_PX，月/年 px/天最小 16/4 致窄于可视区；修复 setCanvasWidth 用 max(canvasW px,100%) #531）③功能区图标统一强调蓝（.btn .ic 补 color:var(--accent)，主按钮白字）。用户用于上传仓库备份。
- 注：未改 Models.cs 版本号（用户未要求升版），文档记为「v2.9.42 补丁 · 2026-09-01」。
